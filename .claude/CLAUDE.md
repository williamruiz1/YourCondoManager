<!-- PPM:START v1 -->
# Pocket PM Protocol (auto-generated by `ppm link`, do not edit by hand)

> Product: **YourCondoManager (YCM)** (`1e2da109-f6f6-431c-8dc0-f61b548a1b83`)
> Schema: 1.0.0 · Manifest v1

## The Loop

1. `mcp__pocketpm__ppm_bootstrap` — your first call every session
2. `mcp__pocketpm__ppm_checkpoint_start` — before each work burst
3. Do the work
4. `mcp__pocketpm__ppm_checkpoint_end` — automatically syncs
5. `mcp__pocketpm__ppm_session_end` — before you stop

## Tool Preference (MANDATORY)

Prefer `mcp__pocketpm__*` tools for all PM state.
- Avoid: shell out to `ppm` unless you have no MCP access
- Avoid: create static feature-map docs and treat them as canonical
- Avoid: read `.pocketpm/CONTEXT.md` as a source of truth

MCP tools route through server-side enforcement (single-open-session, freshness checks, blocker scans). Shell commands bypass that layer.

## Protocol Directives

### loop-order

Every session follows this order:

1. `ppm_bootstrap` — fetch live PM state (tasks, blockers, tree)
2. `ppm_checkpoint_start` — open a named work burst before editing
3. (do the work)
4. `ppm_checkpoint_end` — automatically syncs; runs blocker scan
5. `ppm_session_end` — required before you stop

_enforced by: mcp, hooks, convention_

### tool-preference

Always prefer `mcp__pocketpm__*` tools. The shell `ppm` CLI exists for humans, not agents.

_enforced by: convention_

### context-staleness

`.pocketpm/CONTEXT.md` is a regenerable cache with a STALE AFTER header. Never treat it as canonical.

_enforced by: mcp, convention_

## Recovery — if `mcp__pocketpm__*` tools are not available

This happens when the workspace was wiped (Replit/Gitpod/Codespaces
container restart) or the install is stale. Run this from the repo
root, then restart Claude Code so the MCP server attaches:

```bash
curl -fsSL https://pocketpm.fly.dev/install.sh | bash -s -- --product-id 1e2da109-f6f6-431c-8dc0-f61b548a1b83
ppm relink
ppm doctor
```

If `ppm doctor` reports missing auth, re-run the installer with `--key <ppm_...>`.

<!-- PPM:END v1 -->
