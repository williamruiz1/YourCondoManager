# PPM Task Card — 2.4 Platform Admin Surface Audit
**PPM Task ID:** `043c1502-ab03-4e3e-a66e-49dae5f9840b`
**Title:** Platform Overhaul 2.4 — Platform Admin surface audit (atomic Phase 16 bundle)
**Status:** queued
**Priority:** high
**Assignee:** Computer
**Layer:** 2 (Role & Permission Model)
**Module:** 2.4
**Upstream dependency:** Phase 15 Communications zone landing complete
**Decision doc:** [`../decisions/2.4-platform-admin-surface-audit.md`](../decisions/2.4-platform-admin-surface-audit.md)
**Handoff:** [`../handoffs/2.4-platform-admin-surface-audit-handoff.md`](../handoffs/2.4-platform-admin-surface-audit-handoff.md)
**Process skill:** [`../../../skills/spec-first-overhaul-process-skill.md`](../../../skills/spec-first-overhaul-process-skill.md)
**Plan reference:** `/home/runner/.claude/plans/floofy-hopping-dusk.md` — **Phase 16 (atomic PR)**
**Spec status:** SPEC LOCKED (2026-04-15)
**Created:** 2026-04-20

---

## Objective

Ship the Platform zone landing as a single atomic PR: narrow every `/api/platform/*`, `/api/admin/*`, `/api/ai/ingestion/*` handler to `requireAdminRole(["platform-admin"])`; wrap every `/app/platform/*`, `/app/admin/*`, `/app/ai/*` client route with `<RouteGuard roles={["platform-admin"]}>`; narrow `platformSubPages` tab-bar entries to platform-admin only; remove the Owner Portal launcher from platform nav; guarantee server + UI roll back together. The Platform zone is the final 3.3 zone landing and depends on Phase 15 (Communications) being complete and green.

---

## Acceptance Criteria (verbatim / tightly paraphrased from decision doc)

1. 0.2 is amended in a separate commit to add Persona 4 — Platform Admin; Persona Boundary Matrix extended with a Platform Admin column; 2.4 does not define a standalone platform-admin boundary artifact separate from 0.2 (Q1)
2. Every `/app/platform/*`, `/app/admin/*`, `/app/ai/*` route in `client/src/App.tsx` is wrapped by `<RouteGuard roles={["platform-admin"]}>`; no route relies on server 403 alone for client gating; `canAccessWipRoute` consolidated into centralized `<RouteGuard>` per 2.3 Q9; no inline `adminRole ===` ternaries remain at the route level (Q2)
3. All `/api/platform/*`, `/api/admin/*`, `/api/ai/ingestion/*` read and write handlers gate to `requireAdminRole(["platform-admin"])`; the widened-reads enumerated in the Cross-Reference Notices are narrowed; no read or write accepts `board-officer`, `assisted-board`, `pm-assistant`, `manager`, or `viewer` (Q3)
4. `client/src/App.tsx:177-180` `platformSubPages` entries all gate to `roles: ["platform-admin"]` only; the `board-admin` (old role) entries on Admin Roadmap and Executive are removed; tab-bar role lists match the sidebar group `["platform-admin"]` gate across every platform surface (Q4)
5. `client/src/components/app-sidebar.tsx:185` Owner Portal launcher is removed; `client/src/App.tsx:180` equivalent `platformSubPages` Owner Portal entry is removed; no `/app/*` nav surface contains a link to `/portal` after this resolution; Platform Admin users authenticate via portal-access-id in a separate session (Q5)
6. 1.1 is amended in a separate commit to add a sixth zone, Platform, covering `/app/platform/*`, `/app/admin/*`, `/app/ai/*`; the 1.1 zone-assignment table includes all five platform page files with `zone: Platform` and `persona: Platform Admin`; no route-tree split to `/operator/*` is introduced (Q6)
7. Every diagnostic/observability endpoint under `/api/platform/*` and `/api/admin/*` narrows to `requireAdminRole(["platform-admin"])`; no ad-hoc cross-role read exposure remains without explicit spec approval; any future approval cites 0.3 Q5 AC 22 (Q7)

---

## Build Gate

Computer may not begin implementation until a handoff doc exists at:

```
docs/projects/platform-overhaul/handoffs/2.4-platform-admin-surface-audit-handoff.md
```

The handoff doc enumerates the per-surface evidence table, the atomic-PR bundle contents (server narrowing + tab-bar narrowing + launcher removal + RouteGuard wrappers), the revert procedure, and the validation steps. Until that file exists, this task remains `queued`.

Phase 15 must be green before Phase 16 begins. Per 3.3 Q8, all four changes ship in a single PR so revert is atomic.

---

## Status Transitions

| Transition | When | Who |
|---|---|---|
| `queued → assigned` | Handoff doc published, Phase 15 green, Computer activated at Phase 16 | YCM Command Center |
| `assigned → in_progress` | Computer calls `ppm_start_work` | Computer |
| `in_progress → review` | Atomic PR open with all four changes, parity harness passes, manual smoke complete, ACs met | Computer (via `ppm_workitem_transition`) |
| `review → approved` | PM approves on dashboard | PM (not agent-side) |
| `approved → done` | PM closes | PM (not agent-side) |

On `in_progress` start, update module 2.4 status in `00-index.md` to `IN BUILD`. On `review`, update to `IN REVIEW`. On PM approval, update to `COMPLETE`.
