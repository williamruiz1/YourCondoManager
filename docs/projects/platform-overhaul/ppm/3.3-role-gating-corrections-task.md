# PPM Task Card — 3.3 Role-Gating Corrections
**PPM Task ID:** `7b23d9c6-b087-4063-9c70-ec3e731c7963`
**Title:** Platform Overhaul 3.3 — Role-gating corrections (5 zone landings + parity harness)
**Status:** queued
**Priority:** high
**Assignee:** Computer
**Layer:** 3 (Navigation Restructure)
**Module:** 3.3
**Upstream dependency:** Phase 0b ADR (shared manifest), Phase 8c primitives (RouteGuard, useIsReadOnly, AdminRole export), Phase 9 persona-access manifest populated
**Decision doc:** [`../decisions/3.3-role-gating-corrections.md`](../decisions/3.3-role-gating-corrections.md)
**Handoff:** [`../handoffs/3.3-role-gating-corrections-handoff.md`](../handoffs/3.3-role-gating-corrections-handoff.md)
**Process skill:** [`../../../skills/spec-first-overhaul-process-skill.md`](../../../skills/spec-first-overhaul-process-skill.md)
**Plan reference:** `/home/runner/.claude/plans/floofy-hopping-dusk.md` — **Phase 10 (harness); Phases 12/13/14/15/16 (5 zone landings)**
**Spec status:** SPEC LOCKED (2026-04-16)
**Created:** 2026-04-20

---

## Objective

**This is a PARENT card.** 3.3 owns the order-of-operations that takes the repo from its current mixed state (BoardPortal shunt mounted, `normalizeAdminRole` active, `canAccessTab` null-role short-circuit, widened platform API role arrays) to the 2.x-compliant target without breaking any of the 544 handlers catalogued in 2.1 Q9. Consume locked resolutions from 2.1, 2.2, 2.3, 2.4 and sequence them into: (Phase 10) parity harness + portal role enum collapse + null-role fix + `AdminRole` canonical export, then (Phases 12–16) five sequential zone landings (Financials → Operations → Governance → Communications → Platform), each with per-zone RouteGuard wrappers, per-zone tests, manual role smoke, and atomic rollback.

Future splits into per-zone child cards may be considered if coordination overhead warrants — current scope keeps this as a single parent card.

---

## Acceptance Criteria (verbatim / tightly paraphrased from decision doc)

1. Canonical landing sequence: portal role enum collapse → `canAccessTab` null-role fix → `PortalRequest` retyping / `requirePortalBoard*` retirement → `AdminRole` canonical export → RouteGuard rollout (zone-by-zone Financials→Operations→Governance→Communications→Platform) → BoardPortal shunt retirement (rides Operations) → `normalizeAdminRole` migration/deletion (rides Governance) → Platform API + UI narrowing (rides Platform) (Q1)
2. `portalAccessRoleEnum` contains only `["owner"]` post-migration; `PortalRequest.portalRole` / `portalEffectiveRole` typed as `"owner"`; `requirePortalBoard` + `requirePortalBoardReadOnly` deleted; zero `portal_accesses.role` rows with non-canonical values; `isSingleAssociationBoardExperience` collapsed to one shared definition; all four parallel `AdminRole` re-declarations replaced with imports from `shared/` (Q2)
3. `canAccess()` at `app-sidebar.tsx:209-213` returns `false` when role is nullish; session-loading guard suppresses sidebar render until auth resolves; unit test covers `canAccess(null/undefined, roles)` returning `false` (Q3)
4. Parity harness: Tier 1 exhaustive for every platform-admin gate route against all six personas; Tier 1 covers PM Assistant exclusion and Owner portal isolation; Tier 2 covers at least one representative route per 1.1 zone; Tier 3 sidebar smoke across all six personas; all Tier 1 + at least one Tier 2 per zone pass before RouteGuard rollout begins (Q4)
5. Each zone ships as a single PR with all `<RouteGuard>` wrappers for that zone; landing order Financials→Operations→Governance→Communications→Platform; no zone merges until previous zone's Q9 gates + Q11 observability pass; each zone PR includes the parity-harness tests for that zone (Q5)
6. Feature flag `BOARD_SHUNT_ACTIVE` wraps the shunt; when `false`, `board-officer` / `assisted-board` sessions render in `WorkspaceShell`; flag defaults to `true`, flipped only after Operations zone RouteGuard landing; code comment citing `2.3` added at removal commit; post-removal no `BoardAdminPortalShell` / `BoardPortal` entry-point references remain; E2E test verifies `board-officer` reaches `WorkspaceRouter` per 0.2 matrix (Q6)
7. Production data audit enumerates every `admin_users.role` value; migration converts all alias rows to canonical; zero alias rows remain; `normalizeAdminRole` deleted; silent `unknown → viewer` downgrade eliminated; ESLint rule prevents non-canonical role string assignments; no call site references `normalizeAdminRole` (Q7)
8. All four changes (server narrowing + tab-bar narrowing + launcher removal + RouteGuard wrappers) ship in a single PR (Platform zone landing); all `/api/platform/*`, `/api/admin/*`, `/api/ai/ingestion/*` gate to `["platform-admin"]`; no `board-admin` entries in `platformSubPages`; no `/app/*` nav surface links to `/portal`; every platform/admin/ai route wrapped by `<RouteGuard roles={["platform-admin"]}>`; revert restores all four surfaces atomically (Q8)
9. Each zone landing PR includes passing parity-harness results (Tier 1 + Tier 2 for that zone); `tsc --noEmit` error count does not increase vs baseline; manual smoke documented in PR description; no landing merges with a failing Tier 1 test (Q9)
10. Each zone landing is a single PR revertible independently; revert trigger criteria documented in PR template; portal role migration includes a reverse migration script; two or more consecutive zone failures trigger full pause (Q10)
11. Post-deploy checklist includes three gates (zero 401/403 on permitted routes, zero unguarded access on blocked routes, no JS console error increase); each zone PR has a post-merge comment with gate results; any failure triggers the Q10 revert (Q11)
12. Each zone landing produces a one-page handoff doc under `docs/overnight/` in the canonical template format (Q12)

---

## Build Gate

Computer may not begin implementation until a handoff doc exists at:

```
docs/projects/platform-overhaul/handoffs/3.3-role-gating-corrections-handoff.md
```

The handoff doc enumerates the Phase 10 harness scope, the five-zone landing sequence, the per-zone file inventory, the rollback playbook, and the observability gates. Until that file exists, this task remains `queued`.

Phase 8c primitives (RouteGuard, useIsReadOnly, shared `AdminRole`) and Phase 9 manifest must be complete before Phase 10 harness work begins.

---

## Status Transitions

| Transition | When | Who |
|---|---|---|
| `queued → assigned` | Handoff doc published, Phase 8c + Phase 9 complete, Computer activated at Phase 10 | YCM Command Center |
| `assigned → in_progress` | Computer calls `ppm_start_work` | Computer |
| `in_progress → review` | All five zone landings merged, parity harness green, Q11 observability gates pass on every landing, ACs met | Computer (via `ppm_workitem_transition`) |
| `review → approved` | PM approves on dashboard | PM (not agent-side) |
| `approved → done` | PM closes | PM (not agent-side) |

On `in_progress` start, update module 3.3 status in `00-index.md` to `IN BUILD`. On each zone landing merge, update `00-index.md` progress notes. On final review, update to `IN REVIEW`. On PM approval, update to `COMPLETE`.
