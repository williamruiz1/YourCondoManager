# PPM Task Card — 3.5 Owner Portal Restructure (BUILD)
**PPM Task ID:** `ca6972a3-6b9e-4620-9358-c1cc6c9f42e3`
**(Skeleton card:** `65ae5d6b-9eee-407d-ac7e-c3fdd64aa67c`**)**
**Title:** Platform Overhaul 3.5 BUILD — Owner Portal restructure (mega-file decomposition)
**Status:** queued (BLOCKED pending SPEC LOCK)
**Priority:** medium
**Assignee:** Computer (post-SPEC LOCK)
**Layer:** 3 (Navigation Restructure)
**Module:** 3.5
**Upstream dependency:** Phase 3 (1.4) + Phase 11 (3.1 sidebar) shipped
**Decision doc:** [`../decisions/3.5-owner-portal-restructure.md`](../decisions/3.5-owner-portal-restructure.md) — **IN SPEC skeleton; Q1–Q8 PENDING; module NOT SPEC LOCKED**
**Handoff:** [`../handoffs/3.5-owner-portal-restructure-handoff.md`](../handoffs/3.5-owner-portal-restructure-handoff.md)
**Process skill:** [`../../../skills/spec-first-overhaul-process-skill.md`](../../../skills/spec-first-overhaul-process-skill.md)
**Plan reference:** `/home/runner/.claude/plans/floofy-hopping-dusk.md` — **Phase 18 (T1)**
**Spec status:** IN SPEC (skeleton — Q1–Q8 PENDING)
**Created:** 2026-04-20

---

## Objective

> ⚠️ **BLOCKED pending 3.5 SPEC LOCK.** Build cannot begin until Q1–Q8 resolve — file founder session to resolve. The skeleton card `65ae5d6b-9eee-407d-ac7e-c3fdd64aa67c` tracks the spec-resolution work. This card (`ca6972a3…`) is the BUILD card that activates only once the eight open questions resolve and the module advances to SPEC LOCKED.

Decompose `client/src/pages/owner-portal.tsx` (currently 3,935 LOC, one mega-file with 7 tab-switched views driven by local `activeTab` state) into route-per-zone components per the locked 1.1 Q5 four-zone vocabulary (Home, My Finances, My Requests, My Community) and the locked 1.2 Q4 hub URLs (`/portal/finances`, `/portal/requests`, `/portal/community`, plus `/portal` dashboard). Migrate the interim 4.2 Q5 `useDocumentTitle` sprinkles into per-route components. Implement the 4.2 Q4 HYBRID Community integration: `/portal/community` renders an inline preview (unread notices, upcoming events, recent posts) with click-through to `/community/:associationId` for deep engagement. Supersede the 2.2 Q6 in-place session-gate patch by relocating the gate to the chosen shell/route pattern (Q4-dependent).

The eight PENDING questions that must resolve before build:
- Q1 — route tree depth (flat hubs vs sub-page mirroring of `/app/*`)
- Q2 — component split granularity (one file per zone vs finer)
- Q3 — shared layout (`PortalShell` vs thinner `PortalLayout`)
- Q4 — session gate placement (shell-owned vs per-route)
- Q5 — amenities inline vs linked (coupled to 4.2 Q3 PENDING)
- Q6 — Community inline preview component (new vs reuse of public Hub widgets)
- Q7 — migration strategy (big-bang vs incremental)
- Q8 — deep-link preservation for existing `/portal` tab-deep-links

---

## Acceptance Criteria (from decision doc)

**TBD after SPEC LOCK.** The 3.5 decision doc explicitly states no AC is enumerated in the skeleton — attempts to pre-enumerate would pre-commit the Qs. Anchors for downstream AC derivation (from parent 4.2 Q1 AC 3):

1. Decomposition of `client/src/pages/owner-portal.tsx` into route-per-zone components (anchor 4.2 Q1 AC 3a)
2. Introduction of `/portal/finances`, `/portal/requests`, `/portal/community` routes per 1.2 Q4 (anchor 4.2 Q1 AC 3b)
3. Sidebar label migration to 1.1 Q5 four-zone vocabulary (Home, My Finances, My Requests, My Community) (anchor 4.2 Q1 AC 3c)
4. `useDocumentTitle` migration from interim Phase 3 in-place patches to the new per-route components (anchor 4.2 Q1 AC 3d)
5. Portal parity verification harness per 3.3 Q4 pattern (anchor 4.2 Q1 AC 3e — future task)
6. 4.2 Q4 HYBRID Community integration: inline preview inside `PortalCommunity.tsx` plus click-through to `/community/:associationId` (derivation of implementation component — Q6-dependent)
7. 2.2 Q6 session-gate fix for `/portal/amenities` relocated from the in-place patch to the chosen architecture (Q4-dependent)
8. The interim `useDocumentTitle` tab-switch sprinkle (4.2 Q5 AC-10) deleted with the mega-file

Full AC set will be written into this card once Q1–Q8 resolve and the decision doc advances to SPEC LOCKED.

---

## Build Gate

Computer may not begin implementation until **both**:

1. The 3.5 decision doc advances from IN SPEC to SPEC LOCKED (Q1–Q8 all resolved), AND
2. A handoff doc exists at:

```
docs/projects/platform-overhaul/handoffs/3.5-owner-portal-restructure-handoff.md
```

The handoff doc enumerates the chosen split pattern, the per-file inventory of new components, the migration strategy (big-bang vs incremental per Q7), the deep-link compat layer (per Q8), and the validation steps.

Per `ppm_human_task_create` convention: the SPEC LOCK gate is a William-action item — file a human task to schedule the founder Q1–Q8 resolution session. Do not guess answers to the eight PENDING questions.

---

## Status Transitions

| Transition | When | Who |
|---|---|---|
| `queued (BLOCKED) → queued` | 3.5 decision doc advances to SPEC LOCKED after Q1–Q8 resolved | YCM Command Center |
| `queued → assigned` | Handoff doc published, Phase 3 + Phase 11 shipped, Computer activated at Phase 18 | YCM Command Center |
| `assigned → in_progress` | Computer calls `ppm_start_work` | Computer |
| `in_progress → review` | Mega-file decomposed, per-route components shipped, parity harness green, ACs met | Computer (via `ppm_workitem_transition`) |
| `review → approved` | PM approves on dashboard | PM (not agent-side) |
| `approved → done` | PM closes | PM (not agent-side) |

On SPEC LOCK, update module 3.5 status in `00-index.md` from `IN SPEC` to `SPEC LOCKED`. On `in_progress` start, update to `IN BUILD`. On `review`, update to `IN REVIEW`. On PM approval, update to `COMPLETE`.
