# PPM Task Card — 0.1 Dashboard Resolution
**PPM Task ID:** `a304717b-b18b-4163-9928-5a9ea235d3a7`
**Title:** Platform Overhaul 0.1 — Dashboard Resolution (Home / Portfolio Health / Operations Overview)
**Status:** queued
**Priority:** high
**Assignee:** Computer
**Layer:** 0 (Foundational Decisions)
**Module:** 0.1
**Upstream dependency:** none (Layer 0 root)
**Decision doc:** [`../decisions/0.1-dashboard-resolution.md`](../decisions/0.1-dashboard-resolution.md)
**Process skill:** [`../../../skills/spec-first-overhaul-process-skill.md`](../../../skills/spec-first-overhaul-process-skill.md)
**Spec status:** SPEC LOCKED (2026-04-14)
**Created:** 2026-04-14

---

## Objective

Resolve the three-dashboard conflict between `dashboard.tsx` (`/app`), `portfolio.tsx` (`/app/portfolio`), and `operations-dashboard.tsx` (`/app/operations/dashboard`) per Option 1 of the locked spec. Reposition `/app` as "Home" (today/action surface), rename `/app/portfolio` to "Portfolio Health" as a subordinate analytical view, and rename `/app/operations/dashboard` to "Operations Overview" (Z1-6 section hub). Move portfolio-level stat cards from Home to Portfolio Health, add cross-association operational alerts to Home, and fix the stale billing quick-action link. No route paths, API contracts, or redirect behaviors change.

---

## Acceptance Criteria (verbatim from decision doc)

1. Navigating to `/app` renders a page with nav label and header "Home" — the word "Dashboard" does not appear in the nav label, page title, or breadcrumb for this page
2. The Home page does not contain portfolio-level aggregate stat cards (total associations, units, owners, tenants, board members, documents)
3. The Home page contains a visible link or summary widget pointing to `/app/portfolio`
4. The Home page alerts panel surfaces overdue work orders and due maintenance schedule instances regardless of the currently active association context
5. Navigating to `/app/portfolio` renders a page titled "Portfolio Health" that includes the aggregate stat cards removed from Home
6. "Portfolio Health" appears in the sidebar as a subordinate item to "Home" — not at the same nav level
7. Navigating to `/app/operations/dashboard` renders a page titled "Operations Overview" — the word "Dashboard" does not appear in the page title, breadcrumb, or sidebar nav label for this page
8. A `board-admin` user with exactly one association navigating to `/app/portfolio` is redirected to `/app` — behavior preserved
9. `App.tsx` contains an explicit inline comment at the board-admin redirect logic explaining the intent
10. The quick-actions panel on Home does not contain any link to a legacy redirect path; the billing quick action points directly to `/app/financial/billing`

---

## Build Gate

Computer may not begin implementation until a handoff doc exists at:

```
docs/projects/platform-overhaul/handoffs/0.1-dashboard-resolution-handoff.md
```

The handoff doc enumerates the specific files to read, the ordered edit chunks, and the validation steps. Until that file exists, this task remains `queued`.

No product decisions may be inferred beyond the SPEC LOCKED decision doc. If a question arises that the decision doc does not answer, surface it in a session handoff or via `ppm_human_task_create` — do not guess.

---

## Status Transitions

| Transition | When | Who |
|---|---|---|
| `queued → assigned` | Handoff doc published, Computer activated | YCM Command Center |
| `assigned → in_progress` | Computer calls `ppm_start_work` | Computer |
| `in_progress → review` | Build complete, all acceptance criteria met, session summary written | Computer (via `ppm_workitem_transition`) |
| `review → approved` | PM approves on dashboard | PM (not agent-side) |
| `approved → done` | PM closes | PM (not agent-side) |

On `in_progress` start, update module 0.1 status in `00-index.md` to `IN BUILD`. On `review`, update to `IN REVIEW`. On PM approval, update to `COMPLETE`.
