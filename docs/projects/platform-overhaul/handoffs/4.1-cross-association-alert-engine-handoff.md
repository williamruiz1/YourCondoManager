# Handoff — 4.1 Cross-Association Alert Engine

**Module:** 4.1 — Cross-Association Alert Engine
**Status:** SHIPPED via Waves 11–19 (multiple PRs).
**No active build handoff needed** — this file exists to satisfy the 00-index handoff column requirement and points to the shipped implementation.

**Decision doc:** [`../decisions/4.1-cross-association-alert-engine.md`](../decisions/4.1-cross-association-alert-engine.md)

---

## Shipped artifacts

- **Server engine:** `server/alerts/` (index, types, can-access-alert, sources, cache, schedulers).
- **Source types (Tier 1 + Tier 2 partial):** overdue work orders, due maintenance, active elections, delinquent ledger balances, expiring governance docs, budget variance flags, insurance expiry, unpaid late fees, vendor contract renewals.
- **Single aggregation endpoint:** `GET /api/alerts/cross-association` (60 s cache + targeted invalidation).
- **`HubAlertWidget`:** count badge + top-3 mini-list + "All clear" empty state, persona-invariant. Mounted on the four zone hubs (PR #29 `885912b`).
- **Real-time cache invalidation:** 9 source-type write paths fire targeted invalidates (PR #45 `7979d59`).
- **Batch alert fan-out (perf F1):** Wave 19 (PR #46 `d29502d`).
- **Inbox surface:** central inbox at `/app/communications/inbox` with three-tab filter + restore mutation + 120s polling (PR #26 `2074f0c`).
- **Tests:** `server/alerts/__tests__/` covers each source type + cross-association endpoint + access-control predicate.

## What remains (filed as follow-up, not part of this row)

- **Tier 3 sources** (excluded from 4.1 v1 per Q1 resolution).
- **Server push** (excluded — Q8 chose hybrid fetch-on-navigate + 120 s polling for Home + inbox only).

## Status

The decision doc is SPEC LOCKED. Implementation tracks SHIPPED. Module 4.1 stays at SPEC LOCKED in 00-index until a Layer-4 status column is introduced.
