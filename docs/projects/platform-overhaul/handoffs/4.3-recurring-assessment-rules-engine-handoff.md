# Handoff — 4.3 Recurring Assessment Rules Engine

**Module:** 4.3 — Recurring Assessment Rules Engine
**Status:** SHIPPED via Waves 7, 11–19 (multiple PRs). Legacy posters deleted in Wave 12.
**No active build handoff needed** — this file exists to satisfy the 00-index handoff column requirement.

**Decision doc:** [`../decisions/4.3-recurring-assessment-rules-engine.md`](../decisions/4.3-recurring-assessment-rules-engine.md)

---

## Shipped artifacts

- **Q3 unified pipeline:** `server/assessment-execution.ts` orchestrator + `server/assessment-execution-parity.ts` shadow-write harness + `assessmentRunLog` audit table. `ASSESSMENT_EXECUTION_UNIFIED` flag flipped to default ON in Wave 12 (Phase 5.1 cleanup); legacy posters (`runDueRecurringCharges`, `processSpecialAssessmentInstallments`) deleted.
- **Q5 owner-portal expansion:** recurring summary + upcoming special-assessment installments + per-assessment drill-in surface (label, principal, interest rate, term, allocation method, owner's portion, payment options, total interest). Schema extensions on `specialAssessments` + `server/assessment-ownership.ts` calc module + `GET /api/portal/assessments/:assessmentId/detail`. Shipped via PR #27 `ac76808` `feat(4.3): owner portal assessment detail surface + per-owner-portion calc + dashboard extension`.
- **Q6 full rule visibility for Assisted Board (no write):** `assessment_rules_write` PM toggle introduced.
- **Q7 canonical rule model:** `assessmentFrequencyEnum` harmonized; `graceDays` + `startDate`/`endDate`; `unitScopeMode` enum + inclusion/exclusion/unit-type-filter; flat amount retained.
- **Q8 batch + on-demand unified:** `POST /api/financial/rules/:ruleId/run` with `dryRun`; legacy per-subsystem endpoints became shims.
- **Q9 consolidated UI:** `/app/financial/rules` (Recurring / Special Assessments / Run History tabs) + run-history endpoint + legacy-banners (PR #28 `0498e68`).
- **Tests:** parity harness + per-source vitest suites cover the orchestrator + ownership calc + drill-in endpoint.

## What remains (filed as follow-up)

- **Q1 — `hoaFeeSchedules` retirement** — tracked in PPM workitem `bc8aa43f` "Retire hoaFeeSchedules — migrate data into recurringChargeSchedules". Out of 4.3 row scope.
- **`ASSESSMENT_EXECUTION_UNIFIED` flag removal** — scheduled once every env runs with the flag ON across one full monthly billing cycle. Per-association override still supported until then.

## Status

Module 4.3 stays at SPEC LOCKED in 00-index. All 4.3-owned tracks shipped or filed as follow-up.
