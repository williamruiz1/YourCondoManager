# Handoff — 0.1 Dashboard Resolution
**Module:** 0.1 — Dashboard Resolution
**Layer:** 0
**PPM Task:** `a304717b-b18b-4163-9928-5a9ea235d3a7` ([task card](../ppm/0.1-dashboard-resolution-task.md))
**Decision doc:** [`../decisions/0.1-dashboard-resolution.md`](../decisions/0.1-dashboard-resolution.md) (SPEC LOCKED 2026-04-14)
**Assignee:** Computer
**Created:** 2026-04-14

---

## Before You Start — Required Reading

Read these in order. Do not begin edits until all five are read:

1. `AGENTS.md` (repo root)
2. `docs/agent-bootstrap/workspace-manifest.json`
3. `docs/skills/spec-first-overhaul-process-skill.md`
4. `docs/projects/platform-overhaul/00-index.md`
5. `docs/projects/platform-overhaul/decisions/0.1-dashboard-resolution.md`

Then call `mcp__pocketpm__ppm_bootstrap`, then `mcp__pocketpm__ppm_start_work` with `taskId: a304717b-b18b-4163-9928-5a9ea235d3a7`. On start, update module 0.1 status in `00-index.md` from `SPEC LOCKED` to `IN BUILD` in the same commit that opens work.

No product decision that is not present in the decision doc may be inferred. If you encounter ambiguity, stop and file `ppm_human_task_create` instead of guessing.

---

## Files You Will Touch

| Path | What changes |
|---|---|
| `client/src/components/app-sidebar.tsx` | Rename sidebar label "Dashboard" → "Home" (around line 69). Rename "Operations Dashboard" → "Operations Overview" (around line 144). Reposition "Portfolio Health" as subordinate to "Home" — not a peer. |
| `client/src/pages/dashboard.tsx` | Remove portfolio-level stat cards (total associations, units, owners, tenants, board members, documents). Update page header to "Home". Add Portfolio Health summary widget or link row pointing to `/app/portfolio`. Add cross-association operational alerts (overdue work orders, due maintenance schedule instances) that surface regardless of active-association context. Fix the stale quick-action link: label "Billing" and href `/app/financial/billing` (currently points at legacy `/app/financial/ledger`). |
| `client/src/pages/portfolio.tsx` | Update page title to "Portfolio Health". Add the stat cards removed from Home as the header summary row. No other content changes. |
| `client/src/pages/operations-dashboard.tsx` | Update page title and header to "Operations Overview". Update breadcrumb so the word "Dashboard" does not appear. No content changes beyond labels. |
| `client/src/App.tsx` | At the `/app/portfolio` route (around line 300–301), add an inline comment documenting that the `singleAssociationBoardExperience` redirect of Board Officer / Assisted Board users (old `board-admin` role) with ≤1 association to `/app` is intentional and spec-locked by 0.1, not a bug. |

Breadcrumb sources: locate whatever renders the breadcrumb for these three pages (likely a shared breadcrumb component or page-level header) and update the label strings there. Do not introduce a new breadcrumb system.

---

## Out of Scope (Explicit)

These are NOT part of 0.1 and must not be changed:

- Route paths — `/app`, `/app/portfolio`, `/app/operations/dashboard` all remain. No new routes, no redirects from old paths to new paths (there are no old paths).
- API contracts — no backend changes, no new endpoints, no schema changes.
- Content of Portfolio Health or Operations Overview beyond labels and the stat-card relocation.
- The Board Officer / Assisted Board (old `board-admin`) single-association redirect behavior — it is preserved; only an inline comment is added.
- Persona map, nav model, or any other Layer 0 module. Those are 0.2 and 0.3 and are not yet SPEC LOCKED.

If you notice adjacent issues (broken links elsewhere, stale labels on unrelated pages, TypeScript errors in untouched files), do NOT fix them in this task. File them via `ppm_workitem_create` or note in your session summary.

---

## Validation

Before transitioning the task to `review`, verify each acceptance criterion manually in `npm run dev`:

1. ☐ `/app` renders — header "Home", sidebar label "Home", breadcrumb "Home". Word "Dashboard" appears nowhere on this page.
2. ☐ Home page has no stat cards for total associations, units, owners, tenants, board members, or documents.
3. ☐ Home page has a visible link or widget pointing to `/app/portfolio`.
4. ☐ Home alerts panel shows overdue work orders and due maintenance schedule instances across all associations — confirm by switching the active association in the context switcher and verifying cross-association alerts still appear.
5. ☐ `/app/portfolio` renders with page title "Portfolio Health" and contains the aggregate stat cards that were on the old Home.
6. ☐ Sidebar shows "Portfolio Health" as subordinate to "Home" — not at the same nav level.
7. ☐ `/app/operations/dashboard` renders with title "Operations Overview". Sidebar label, page header, and breadcrumb contain no instance of the word "Dashboard".
8. ☐ A `board-officer` / `assisted-board` user with exactly one association navigating to `/app/portfolio` is redirected to `/app`. Confirm by session-simulating this role or reading the redirect logic is still active.
9. ☐ `client/src/App.tsx` contains an inline comment at the redirect explaining the intent.
10. ☐ Home's quick-actions panel contains no legacy redirect paths. Billing quick-action goes directly to `/app/financial/billing`.

Also run `npm run check` before marking review. Known pre-existing TypeScript issues are documented in `docs/agent-bootstrap/durable-memory.json` under `recurringRepoIssues` — separate those from any failures your changes introduce. If `npm run check` surfaces new failures caused by your edits, fix them before review.

---

## Session Summary — Required

Before stopping, write a session summary to:

```
docs/overnight/2026-MM-DD-0.1-dashboard-resolution-handoff.md
```

Include: acceptance criteria checklist state, files changed, validation results, any surfaced ambiguities, and any adjacent issues filed separately. Then:

1. Transition PPM task `a304717b-b18b-4163-9928-5a9ea235d3a7` from `in_progress` → `review` via `ppm_workitem_transition`
2. Update module 0.1 status in `00-index.md` from `IN BUILD` → `IN REVIEW` in the same commit
3. Call `ppm_sync`
4. Call `ppm_session_end`

PM approves on the pocketp.m. dashboard — do not attempt `approved` or `done` transitions yourself.
