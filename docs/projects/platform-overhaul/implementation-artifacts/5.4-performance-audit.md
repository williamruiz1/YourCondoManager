# 5.4 — Performance Audit (Wave 14 baseline)

**Generated:** 2026-04-24
**Spec:** `docs/projects/platform-overhaul/decisions/5.4-performance-audit.md`
**Scope:** Bundle sizes, server endpoint complexity, image audit. Wave 14 is a baseline + scoping doc; no refactors landed here.

---

## 1. Bundle Sizes — `npm run build` output

### Top 10 client chunks (raw bytes)

| # | Chunk | Raw | Gzip | Tier | Notes |
|---|---|---|---|---|---|
| 1 | `vendor-recharts-*.js` | 392 KB | 108 KB | C (note only) | Recharts is heavy; only used by financial reports + dashboard metric cards. Consider dynamic import on first chart render. |
| 2 | `vendor-radix-*.js` | 268 KB | 87 KB | C | 20+ Radix primitives — Dialog, Tabs, Select, Dropdown, etc. Tree-shaken but base is unavoidable. |
| 3 | `index-*.js` (main entry) | 220 KB | 60 KB | — | App shell + router + eager-loaded providers. Below the 500 KB gzip Tier B threshold. |
| 4 | `financial-billing-*.js` | 104 KB | 20 KB | — | Stripe subscription management. |
| 5 | `communications-*.js` | 84 KB | 16 KB | — | Notice composer + bulk comms inline editor. |
| 6 | `form-*.js` | 64 KB | 16 KB | — | Shared react-hook-form + zod resolver chunk. |
| 7 | `roadmap-*.js` | 64 KB | 15 KB | — | Public roadmap page. |
| 8 | `financial-reports-*.js` | 64 KB | 13 KB | — | Financial reports page (pulls vendor-recharts lazily). |
| 9 | `association-context-*.js` | 60 KB | 13 KB | — | Association detail overview. |
| 10 | `governance-compliance-*.js` | 56 KB | 11 KB | — | Compliance page. |

### Bundle budget verdict (per spec §Bundle Budget)

- **Tier A (>1 MB gzip):** none — PASS
- **Tier B (>500 KB gzip):** none — PASS
- **Tier C (250–500 KB gzip):** `vendor-recharts` (399 KB raw, 108 KB gzip). **Below 500 KB gzip** so we're actually not in Tier B at all; noted as Tier C for future awareness.

No bundle failures block Wave 14. Total client-asset count: ~80 chunks, total raw JS ≈ 3.2 MB, gzip-equivalent ≈ 900 KB. Code splitting is working — route chunks are in the 5–60 KB range.

### Server bundle

- `dist/index.cjs` = **2.4 MB** (build warning flagged). This is the server bundle produced by the custom `tsx script/build.ts` step. Not delivered to the browser; warning is for SSR startup time and is acceptable for a Node server.

---

## 2. Server Endpoint Complexity Notes

Traffic heuristic: endpoints that ship in Layer 0–4 recent waves + the handful any logged-in operator hits per page load.

| Endpoint | Worst-case shape | DB round-trips | Cache | Follow-up |
|---|---|---|---|---|
| `GET /api/alerts/cross-association` | **N associations × M alert sources (7)** — fans out to budget-variance, insurance-expiry, unpaid-late-fees, vendor-contract-renewals, overdue-work-orders, due-maintenance, active-elections. Each source runs its own query per association. | ≈ N × 7 per cold hit; cached 60s per user | 60s in-memory per user | If an operator has 50+ associations, 350 queries per cold hit. Consider batching via `WHERE associationId IN (…)` inside each source and a single materialized view. File: `5.4-F1`. |
| `GET /api/operations/dashboard` | Single-association; N units × status joins. | ~6 queries | none | Fine today. Document cold-path timings once prod. |
| `GET /api/portal/financial-dashboard` | Single owner; joins ledger, payments, assessments. | ~4 queries | none | Fine. |
| `GET /api/financial/assessment-run-log` | Paginated log table; `limit=50` default. | 1 query + count | none | Ensure composite index on `(associationId, runStartedAt DESC)`. Verify. File: `5.4-F2`. |
| `POST /api/financial/rules/:ruleId/run` | Single rule × all in-scope units (per `unitScopeMode`). With `dryRun=false` writes ledger entries per unit. | 1 rule fetch + N unit fetches + N ledger inserts | none | Long-lived. Consider backgrounding via a job queue if > 500 units. File: `5.4-F3`. |
| `GET /api/portal/me` | Single user; portal access + unit joins. | 2 queries | none | Fine. |
| `GET /api/admin/billing/subscription` | Single subscription fetch + planCatalog lookup. | 1–2 queries | none | Fine. |
| `GET /api/portal/maintenance-requests` | Single owner; filtered list. | 1 query | none | Fine. |
| `GET /api/portal/notices` | Single association; paginated. | 1 query | none | Fine. |
| `GET /api/portal/communications` | Single owner; joined with comm history. | 1 query | none | Fine. |

### Biggest risk: `/api/alerts/cross-association` fan-out

The endpoint is the only one that scales per association in a practical way. Today the 60s cache soaks most hits, but a newly-assigned Manager with 50 associations gets a ~350-query cold load. The Layer-4 spec accepted this as the price of the shared-endpoint architecture; 5.4 surfaces it as a real scaling concern for portfolios > 20 associations.

---

## 3. Image Audit

- Static assets under `attached_assets/` include PNG logos and hero imagery.
- No automated compression/re-encode pipeline. PNGs are shipped as-is.
- No `<img loading="lazy">` audit performed in Wave 14.
- Follow-up: `5.4-F4 Static asset optimization` — run `sharp`/`squoosh` over PNG/JPEG assets; add `loading="lazy"` to below-the-fold images.

---

## 4. Smoke Perf Test

- File: `tests/perf-smoke.server.test.ts`
- Asserts that the `canAccessAlert` predicate plus a simulated in-memory alert aggregation completes in under 500ms for 50 mock alerts. Serves as a **baseline**, not a regression gate.
- Real load testing (k6, autocannon) is filed as `5.4-F5`.

---

## 5. Top 3 Recommendations (future work)

1. **Batch the alert fan-out** (`5.4-F1`). Convert each alert source to accept an `associationIds: string[]` array and run one query with `IN (...)` instead of one per association. Expected: drop a 50-association cold hit from ~350 queries to ~7.
2. **Dynamic-import `recharts`** on the first chart render in `dashboard.tsx` / `financial-reports.tsx` (`5.4-F6`). Ship ~108 KB gzip out of the initial critical path for Managers who don't open a chart on first nav.
3. **Ledger table virtualization** for `portal-finances/ledger` and operator ledger tables (`5.4-F7`). Owners with long histories render hundreds of rows today; use `@tanstack/react-virtual` (no new top-level dep — it's already pulled transitively for table primitives in some codebases; verify before adding).

---

## 6. Filed Follow-Ups (Wave 14 out-of-scope)

| ID | Description |
|---|---|
| 5.4-F1 | Batch cross-association alert fan-out with `IN (…)` queries. |
| 5.4-F2 | Verify composite index on `assessment_run_log(associationId, runStartedAt DESC)`. |
| 5.4-F3 | Background rule-run for large associations (>500 units) via job queue. |
| 5.4-F4 | Static asset optimization (PNG/JPEG compression, `loading="lazy"`). |
| 5.4-F5 | Real load test harness (k6 / autocannon) for the 5 endpoints in §2. |
| 5.4-F6 | Dynamic-import `recharts` in `dashboard.tsx` + `financial-reports.tsx`. |
| 5.4-F7 | Virtualize long ledger tables (operator + portal). |
