# 5.6 — i18n Scaffolding

**Status:** SPEC LOCKED 2026-04-25 (Wave 21, ship-and-lock pattern)
**Owner:** Platform Overhaul
**Layer:** 5 — Polish & Hardening

---

## Scope

Establish a minimal, dependency-free string registry for the YCM client and migrate the **same 10 surfaces** covered by 5.5 onto it.

The point of this module is _extraction discipline_: every visible English literal on the in-scope surfaces lives in `client/src/i18n/strings.en.ts`, and pages call `t(key)` instead of writing the literal inline. Wave 21 does not translate — it only locks the registry pattern and the ten highest-leverage surfaces. A follow-up wave introduces a real translation engine and a locale switcher.

**In scope:**
- `client/src/i18n/strings.en.ts` — flat `as const` registry of ~150 string keys covering the 10 in-scope surfaces.
- `client/src/i18n/use-strings.ts` — `t(key): string` lookup helper, plus `useStrings()` hook form for future locale-aware reads.
- `client/src/i18n/README.md` — pattern documentation (how to add a string, how to add a locale, why these 10 first, backlog).
- Refactor the 10 surfaces to call `t()` instead of inline literals.
- Tests under `tests/i18n/strings.test.ts` proving uniqueness, lookups, and the missing-key fallback.

**Out of scope:**
- Adding `i18next`, `formatjs`, or any other translation library. The goal is a registry; the engine is a future wave.
- Translating any string. The English locale is the single source of truth this wave.
- Plural rules, gender, or interpolation. Strings stay flat.
- Locale switching, `Accept-Language` parsing, or a user-preference field. Not in scope.
- Migrating Tier 1 / Tier 2 / Tier 3 surfaces beyond the 10 called out above. Backlog tracked in the README.

---

## Acceptance Criteria

1. `client/src/i18n/strings.en.ts` exists, exports a flat `strings` object marked `as const`, and exports the derived `StringKey` union type.
2. `client/src/i18n/use-strings.ts` exists, exports `t(key: StringKey): string` and `useStrings()`. Both return the literal value for known keys; unknown keys return the key itself (safe fallback).
3. Every key in `strings.en.ts` is unique. (Tested.)
4. The ten in-scope surfaces import from `@/i18n/use-strings` and call `t()` for every user-visible literal that was inline before Wave 21:
   - `dashboard.tsx`
   - `communications-inbox.tsx`
   - `hubs/financials-hub.tsx`
   - `hubs/operations-hub.tsx`
   - `hubs/governance-hub.tsx`
   - `hubs/communications-hub.tsx`
   - `financial-rules.tsx`
   - `portal/portal-home.tsx`
   - `portal/portal-finances.tsx`
   - `settings-billing.tsx`
5. `useDocumentTitle()` calls on the in-scope surfaces use `t()` for the page-title input.
6. Status / plan enum labels in `settings-billing.tsx` (`PLAN_LABELS`, `STATUS_LABELS`) are replaced by helper functions that route each enum value through `t()`. The status / plan enums themselves are not translated — only their human-readable labels.
7. Run-history filter values, billing status enums, and inbox tab labels are extracted (these are common high-traffic short strings).
8. `client/src/i18n/README.md` documents the four required topics: how to add a string, how to add a locale, why these 10 surfaces first, and a tiered backlog of remaining surfaces.
9. No new runtime dependencies introduced. `package.json` is unchanged in this wave.
10. Tests under `tests/i18n/strings.test.ts` verify: (a) `strings.en.ts` exports an object, (b) every key resolves to a non-empty string, (c) `t()` returns the registered value for known keys, (d) `t()` falls back to the key for unknown keys, (e) all keys are unique.

---

## Naming Conventions

- Flat dotted keys: `surface.context.element` (`home.alerts.title`, `inbox.empty.unread`).
- `*.title`, `*.subtitle`, `*.body`, `*.cta` for headings, supporting copy, and primary action.
- `*.empty.title` / `*.empty.body` for empty states; `*.error.title` / `*.error.body` for error states.
- `common.*` only when at least two surfaces share the literal. Single-use strings stay scoped under their surface.
- Do **not** translate test IDs, query keys, route paths, or feature-flag names — those are infrastructure, not user copy.

---

## Migration Path

The README under `client/src/i18n/README.md` is the canonical migration playbook. Summary:

1. Future wave: pick a tier (Tier 1 high-traffic surfaces first), extract literals to `strings.en.ts`, ship.
2. Future wave (when product commits): introduce `strings.es.ts` (or equivalent) with the same key shape; TypeScript will refuse partial coverage.
3. Future wave: add a locale provider, swap `t()` to a locale-aware implementation, and (only if needed) swap the registry to `i18next` for plural / interpolation. The `t(key) → string` API is intentionally a subset so that swap is one wrapping layer.

---

## Spec-Lock Notes

- Locked under the **ship-and-lock** pattern: the implementation lands in the same wave as the spec. The decision doc records what was built, not a future intent.
- This module is **SPEC LOCKED · IN BUILD (partial)** in the same sense as 5.5 — the registry exists and 10 surfaces are migrated, but the remaining 60+ surfaces are explicitly deferred to a future wave.
- The follow-up wave that introduces a real translation engine should not commit to a library until product picks the first non-English locale; the choice of library (`i18next` vs `formatjs` vs custom) is downstream of that decision.

---

## Wave 24 — Round 2 extension

**Status:** SHIPPED 2026-04-25
**Branch:** `wave/24-i18n-round-2`

Wave 24 extends the registry from the 10 Wave-21 surfaces to **15 additional operator surfaces**, mirroring the same scope Wave 23 (a11y round 2) touched so the patterns stay aligned across the two extraction passes.

### Surfaces covered (15)

Financial zone (7):

1. `financial-foundation.tsx`        — namespace `financialFoundation.*`
2. `financial-billing.tsx`           — namespace `financialBilling.*`
3. `financial-recurring-charges.tsx` — namespace `financialRecurring.*`
4. `financial-late-fees.tsx`         — namespace `financialLateFees.*`
5. `financial-delinquency.tsx`       — namespace `financialDelinquency.*`
6. `financial-payments.tsx`          — namespace `financialPayments.*`
7. `financial-ledger.tsx`            — namespace `financialLedger.*`

Operations zone (2):

8. `work-orders.tsx`                 — namespace `workOrders.*`
9. `vendors.tsx`                     — namespace `vendors.*`

Board & Governance zone (3):

10. `meetings.tsx`                   — namespace `meetings.*`
11. `elections.tsx`                  — namespace `elections.*`
12. `announcements.tsx`              — namespace `announcements.*`

Records / People zone (3):

13. `documents.tsx`                  — namespace `documents.*`
14. `persons.tsx`                    — namespace `persons.*`
15. `units.tsx`                      — namespace `units.*`

### Shared additions to `common.*`

Wave 24 introduced reusable eyebrow / breadcrumb / action keys that recur across the operator surfaces:

- `common.eyebrow.finance`, `common.eyebrow.operations`, `common.eyebrow.governance`, `common.eyebrow.records`
- `common.crumb.finance`, `common.crumb.operations`, `common.crumb.board`, `common.crumb.dashboard`
- `common.action.save`, `common.action.saving`, `common.action.cancel`, `common.action.create`, `common.action.update`, `common.action.delete`, `common.action.edit`, `common.action.add`, `common.action.importCsv`
- `common.toast.error`

### Extraction discipline (still partial — same as Wave 21)

Wave 24 extracts the **highest-leverage** strings on each surface — page titles, summary copy, eyebrows, breadcrumbs, tab labels, primary action buttons, dialog titles, key empty/error states, and toast titles. Long-tail in-card body copy, inline help text, and form field placeholders are intentionally left for a future tier-3 sweep so this wave stays under the 2,000-LoC budget. The **registry is the single source of truth** for the strings extracted; surfaces still contain inline literals that future waves will lift.

The Wave 21 conventions still apply: don't promote to `common.*` until at least two surfaces share the literal, don't translate test IDs / query keys / route paths, and dynamic strings (numbers, formatted currency, dates, names) stay as inline expressions.

### Tests

- `tests/i18n/strings.test.ts` extends the Wave 21 baseline:
  - asserts the registry has at least **250 keys** after Wave 24,
  - spot-checks anchor keys across all 15 new surfaces,
  - confirms two new keys (`workOrders.title`, `financialBilling.tabs.ledger`) render correctly via `t()`.

### Out of scope (deferred)

- Translation — still English only.
- Locale switcher / `LocaleContext` — unchanged from Wave 21.
- Long-tail string extraction inside the 15 surfaces (table cell formatters, inline tooltips, form-field placeholders, etc.) — flagged for the next round when product commits to a non-English locale.
- The remaining ~50 surfaces from the README backlog — unchanged tiered roadmap.

---

## Wave 27 — Round 3 extension

**Status:** SHIPPED 2026-04-25
**Branch:** `wave/27-a11y-i18n-round-3`

Wave 27 extends the registry from 25 (post-Wave-24) to **33 operator surfaces**. The 8 surfaces extracted here are the round-3 a11y targets (Wave 27 ships a11y + i18n in a single PR), so the registry coverage continues to mirror the audit footprint exactly.

### Surfaces covered (8)

Operations zone (4):

26. `insurance.tsx`               — namespace `insurance.*`
27. `maintenance-schedules.tsx`   — namespace `maintenanceSchedules.*`
28. `inspections.tsx`             — namespace `inspections.*`
29. `resident-feedback.tsx`       — namespace `residentFeedback.*`

Platform zone (3):

30. `executive.tsx`               — namespace `executive.*`
31. `admin-users.tsx`             — namespace `adminUsers.*`
32. `operations-dashboard.tsx`    — namespace `operationsDashboard.*`

Records zone (1):

33. `ai-ingestion.tsx`            — namespace `aiIngestion.*` (page-shell only — sub-tabs deferred)

### Extraction discipline (continued partial-extraction policy)

Wave 27 keeps the same extraction discipline as Waves 21 / 24: page titles, eyebrows, breadcrumbs, summaries, primary action labels, table headers, dialog titles, empty / loading state messages, and key tab labels are extracted; long-tail tooltip / placeholder / form-field labels stay inline pending a translation commitment. New keys: ~120 across the 8 surfaces. No new `common.*` keys were promoted (the existing `common.action.*` / `common.crumb.*` / `common.eyebrow.*` namespaces already covered the shared verbs and zone labels).

### Tests

`tests/i18n/strings.test.ts` raises the baseline:

- the registry must clear **350 keys** after Wave 27,
- spot-check anchor keys for each of the 8 new surfaces,
- six new `t()` round-trip assertions confirm Wave 27 keys resolve to their English values.

### Out of scope (still deferred)

- Form-dialog string extraction inside the Wave 27 surfaces (input placeholders, select options, schedule-asset / inspection-finding form labels) — reserved for **Wave 28** alongside the matching dialog a11y fixes.
- Tier-3 surfaces still queued (`platform-controls.tsx`, `governance-compliance.tsx`, `board-packages.tsx`, `roadmap.tsx`, AI ingestion sub-tabs, owner-portal sub-pages) — extraction tracks the audit cadence.
- Translation, locale switcher, plural / interpolation engine — unchanged.

## Wave 31 — Round 4 extension (2026-04-25)

Round 4 extends the registry to cover four more main-app surfaces, lifting
the registry from 33 → 37 first-class surfaces and from ~470 keys to ~520
keys. The shape stays identical to Waves 21 / 24 / 27 — flat dotted keys,
`as const` tuple, `t()` resolves at module-load time, no plural / ICU /
runtime translation engine.

Surfaces added (4):

| # | Surface | Namespace | File |
|---|---|---|---|
| 34 | Board Members | `board.*` | `client/src/pages/board.tsx` |
| 35 | Board Packages | `boardPackages.*` | `client/src/pages/board-packages.tsx` |
| 36 | Governance Overview | `governance.*` | `client/src/pages/governance.tsx` |
| 37 | Community Hub (Public) | `communityHubPublic.*` | `client/src/pages/community-hub-public.tsx` |

A new shared key `common.saving` was introduced for the in-flight button
copy on the Board Members assign-role dialog. Public marketing surfaces
(`landing.tsx`, `pricing.tsx`, `solutions.tsx`) intentionally remain
un-extracted — brochure copy is translated as a separate workstream when
the product launches a non-English market.

### What's still deferred

- Tier-4 admin surfaces (`platform-controls.tsx`, `roadmap.tsx`,
  `governance-compliance.tsx`) — deep header / form copy. These pages
  received a11y-only fixes in Wave 31; full extraction is held until
  the platform-admin restructure (Phase 16) freezes the surface list.
- Owner-portal sub-pages beyond `portal-home.tsx` and `portal-finances.tsx`.
- Brochure / marketing translations.
