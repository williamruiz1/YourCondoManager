# 5.3 — Mobile Audit

**Status:** SPEC LOCKED (Wave 14, 2026-04-24) · **IN BUILD (partial)**
**Owner:** Platform Overhaul
**Layer:** 5 — Polish & Hardening

---

## Scope

Audit every authenticated operator and portal surface at **375px** (phone) and **768px** (tablet) breakpoints against `docs/projects/mobile-ui-rules.md`. Fix layouts that overflow, clip, or wrap in unreadable ways.

Wave 14 MVP:
- Inventory the surfaces with fixed-width CSS values or `flex`/`grid` layouts that are likely to overflow under 375px.
- Apply breakpoint fixes (mobile-first with `sm:` / `md:` Tailwind prefixes) to the highest-value surfaces: Home, `/portal`, `/app/financial/rules`, `/app/communications/inbox`, `/portal/finances`.
- Add one smoke-test asserting the Home page doesn't measurably overflow at 375px width. Note: `container.scrollWidth <= 375` is a rough proxy — real mobile layout correctness still requires a human pass.
- Ship `implementation-artifacts/5.3-mobile-audit-findings.md` with surfaces audited, fixes applied, and items deferred.

Everything else (automated visual regression, Percy/Chromatic integration, per-surface retrofits at 320px/430px) is out of scope.

---

## Acceptance Criteria

1. `implementation-artifacts/5.3-mobile-audit-findings.md` lists:
   - Surfaces audited (≥8).
   - Fixes applied in Wave 14 (file + 1-line summary each).
   - Items deferred to future work with rationale.
2. At least 3 mobile layout fixes committed in Wave 14 — each mobile-first with `sm:` / `md:` prefixes (no desktop-first `lg:` reverse patterns unless adopting existing convention).
3. `tests/mobile-audit.client.test.tsx` asserts a smoke expectation that the Home-panel-adjacent render path doesn't overflow at 375px width. Test acknowledges this is a proxy, not a pixel-perfect layout check.
4. `docs/projects/mobile-ui-rules.md` is NOT edited — Wave 14 follows the existing rules; any rule amendments are out of scope.
5. No regressions to desktop (`md:` and above) layouts.

---

## Audit Targets (Wave 14)

Priority surfaces pulled from mobile-regression-candidates + session usage:

| Surface | Route | Notes |
|---|---|---|
| Home (Manager default) | `/app` | Alerts panel + recommended actions — already uses responsive shells. |
| Central inbox | `/app/communications/inbox` | Tabs + list. Audit tabs wrap behavior < 375px. |
| Assessment rules | `/app/financial/rules` | 3 tabs + tables — tables need card fallback below `md:`. |
| Portal home | `/portal` | Already uses mobile-section-shell. Verify landing cards. |
| Portal finances | `/portal/finances` | Tables in ledger sub-page need card fallback. |
| Portal requests | `/portal/requests` | Form + list pair — ensure form stacks. |
| Portal notices | `/portal/notices` | Tabs already responsive; spot-check. |
| Zone hubs x4 | `/app/{financials,operations,governance,communications}` | Placeholder pages; mostly static copy. |

---

## Implementation Artifacts

- Audit doc: `docs/projects/platform-overhaul/implementation-artifacts/5.3-mobile-audit-findings.md`
- Test: `tests/mobile-audit.client.test.tsx`
- Fix commits: see Wave 14 PR diff.

---

## Cross-References

- `docs/projects/mobile-ui-rules.md` — the spec this audit enforces.
- `docs/projects/mobile-regression-candidates.md` — prior list of known-risk surfaces.
- 1.3 Breadcrumb implementation: already mobile-collapsed below 768px; do NOT regress.

---

## Wave 18 — Round 2 adoption

**Date:** 2026-04-24

Wave 14 shipped 3 layout fixes + 4 EmptyState refactors on hub placeholders and deferred 5 follow-up items in `implementation-artifacts/5.3-mobile-audit-findings.md`. Wave 18 closes 3 of those 5 follow-ups.

### Mobile fixes shipped in Wave 18

#### 5.3-F1 Operator table card-fallbacks (PARTIAL — Foundation ratified)

**Surface:** Account Activity table inside `client/src/pages/financial-foundation.tsx`.

**Change:** the 8-column "Account Activity" table previously rendered identically at every breakpoint, blowing past 375px on phones. Wave 18 introduces a stacked-card list rendered at `<md` (`hidden md:block` flips the desktop table off below the breakpoint, and a parallel `space-y-3 md:hidden` block renders mobile cards). Each card surfaces:

- Account name + inactive badge + account code/type meta line.
- Right-aligned utilization % badge.
- Three-up grid of Budget / Invoiced / Variance values (negative variance keeps `text-destructive`).

The desktop `<Table>` is unchanged structurally — only its parent now has `overflow-x-auto` so any future column additions stay confined to the card and don't push the page wider than the viewport at `md` and up.

**Recurring Charges + Special Assessments tables (the other two callouts in the deferred item):** already had isMobile-driven mobile-card renders shipped in earlier waves (Phase 8b). Verified in Wave 18 audit — no regression. The deferred item closes against Foundation only.

#### 5.3-F2 Header compression at <640px

**Surface:** the global operator workspace header in `client/src/App.tsx` (`<header>` inside `WorkspaceShell`).

**Change:** below `sm:` (640px) the header now uses `h-12 min-h-12 px-2 py-1.5 gap-1.5` (instead of `h-16 min-h-14 px-3 py-2 gap-2`) so the sidebar trigger + command palette + association switcher + account avatar fit on one row at 375px without the awkward second-line wrap. Above `sm:` the original sizing is preserved verbatim.

The "Notifications" + "Settings" icon buttons + association switcher already collapse to icon-only / w-full responsively inside `HeaderActions`, which keeps everything accessible at 375px after this change. No nav-drawer rewrite required.

#### 5.3-F4 320px floor

**Surfaces:** Home (`client/src/pages/dashboard.tsx`) and User Settings (`client/src/pages/user-settings.tsx`).

**Change:** outer wrapper padding tightened from `p-6` / `px-6 py-10` to `p-3 sm:p-6` / `px-3 py-6 sm:px-6 sm:py-10` so card content does not touch the viewport edge at 320px. Each Home/Settings card already constrains its own width via flexbox + grid — the outer padding was the offender at 320px.

`/portal/*` already used `px-4` in the portal shell at every breakpoint and was verified to render cleanly at 320px without changes. `/app/settings/billing` inherits the same outer padding fix via the `WorkspaceRouter` shell.

One narrow `w-[260px]` Input on `/app/ai/ingestion` (clause search) was relaxed to `w-full sm:w-[260px]` so it doesn't overflow at 320px.

### Mobile fixes intentionally NOT shipped (still deferred)

| Item | Status | Why still deferred |
|---|---|---|
| 5.3-F3 Visual regression harness | DEFERRED | Playwright/Percy/Chromatic at 320/375/390/430/768 is the right long-term answer; integrating it is a multi-day scaffolding effort. Wave 16a Playwright harness focused on real-backend functional flows; visual snapshots are a separate dimension. Stays on the backlog — track under Mobile Optimization. |
| 5.3-F5 Form dialog mobile density | DEFERRED | Recurring-rule creation dialog grid is functional at 375px (just cramped). Lower priority than F1/F2/F4 for typing/tap fatigue, and design-system level (would touch every `<Dialog>` form). Stays deferred. |

### Wave 18 mobile fix summary

- **3 of 5 deferred items shipped** (F1 Foundation ratification, F2 Header compression, F4 320px floor).
- **2 remain deferred** with rationale (F3 visual harness, F5 form-dialog density).

## Wave 28 — Form-dialog mobile density

**Date:** 2026-04-24

Closes the Wave 14 deferred item **5.3-F5 Form dialog mobile density**. Wave 18 left F5 deferred (rationale: design-system level, would touch every `<Dialog>` form). Wave 28 ships the layout-only fixes across 10 high-traffic dialogs.

### What changed (per-dialog)

All fixes are layout-only — no validation, mutation, or form-state changes.

| # | Dialog | File | Fix |
|---|---|---|---|
| 1 | Recurring Charge create | `client/src/pages/financial-recurring-charges.tsx` | DialogContent: `max-w-md` → `max-w-[calc(100vw-2rem)] sm:max-w-md`. Already had `grid-cols-1 sm:grid-cols-2` and `w-full sm:w-auto` action buttons. |
| 2 | Special Assessment create | `client/src/pages/financial-assessments.tsx` | DialogContent viewport clamp + replaced two `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries with `grid-cols-1 sm:grid-cols-2`. |
| 3 | Late Fee Rule create | `client/src/pages/financial-late-fees.tsx` | DialogContent viewport clamp + `grid-cols-1 sm:grid-cols-2` for fee-type/amount + grace-days/max-fee rows. |
| 4 | Delinquency Threshold create | `client/src/pages/financial-late-fees.tsx` | DialogContent viewport clamp + responsive grid for stage/action-type + min-balance/days. Action row → `flex-col-reverse sm:flex-row sm:justify-end`. |
| 5 | Payment Plan create | `client/src/pages/financial-late-fees.tsx` | DialogContent viewport clamp + responsive grid for unit/person/amount/frequency/start-date. Action row → reversed-stack on mobile. |
| 6 | Collections Handoff (Refer Account) | `client/src/pages/financial-late-fees.tsx` | DialogContent viewport clamp + responsive action row. |
| 7 | Work Order create/edit | `client/src/pages/work-orders.tsx` | DialogContent viewport clamp + `md:grid-cols-2` → `grid-cols-1 sm:grid-cols-2` so the 11-field grid stacks below 640px instead of trying to fit two columns at 375px. |
| 8 | Document Upload | `client/src/pages/documents.tsx` | DialogContent viewport clamp on the 3 dialogs (`max-w-3xl` upload + `max-w-3xl` view + `max-w-md` confirm). The internal `md:grid-cols-[1.1fr_0.9fr]` two-column was already responsive (stacks below md). |
| 9 | Admin User invite | `client/src/pages/admin-users.tsx` | DialogContent viewport clamp on the `max-w-lg` shell. |
| 10 | Election create + Tally + Ballot Options + Proxy | `client/src/pages/elections.tsx` | DialogContent viewport clamp on all 4 dialog shells. Replaced 4 unconditional `grid-cols-2 gap-3` blocks with `grid-cols-1 sm:grid-cols-2 gap-3` — the create-election dialog was the worst offender (Vote Type / Voting Rule, Opens At / Closes At, Quorum % / Visibility) at 375px. Action row → `flex-col-reverse sm:flex-row`. |
| 11 | Maintenance Schedule + Asset Registry | `client/src/pages/maintenance-schedules.tsx` | DialogContent viewport clamp on both dialogs. Schedule + Asset internal grids were `md:grid-cols-2` → `grid-cols-1 sm:grid-cols-2` (a 12-field grid was packing two columns at 375px). Action rows → reversed-stack on mobile. |
| 12 | Vendor create/edit | `client/src/pages/vendors.tsx` | DialogContent viewport clamp + `md:grid-cols-2` → `grid-cols-1 sm:grid-cols-2` for the 9-field vendor info grid. |
| 13 | Building / Association create | `client/src/pages/associations.tsx` | DialogContent viewport clamp on both dialogs (association create + view). Replaced `${isMobile ? "grid-cols-1" : "grid-cols-2"}` for City/State row with `grid-cols-1 sm:grid-cols-2`. |
| 14 | Persons (Owner / Resident) create + Board Role | `client/src/pages/persons.tsx` | DialogContent viewport clamp on both. Replaced 4 `isMobile` grid ternaries with `grid-cols-1 sm:grid-cols-2` (first/last name, role/unit, board start/end dates). Board-role action button → `w-full sm:w-auto`. |

That's **14 distinct dialogs across 10 files**, well above the 8–10 floor. The dialog count exceeds the file count because three pages (`financial-late-fees.tsx`, `elections.tsx`, `maintenance-schedules.tsx`) host multiple form dialogs that all needed the same treatment.

### Patterns applied

1. **Viewport clamp:** every fixed-width DialogContent now reads `max-w-[calc(100vw-2rem)] sm:max-w-{lg|xl|2xl|3xl}`. Below `sm:` (640px) the dialog can never exceed `viewport - 2rem` (the body has `p-6` ≈ 1.5rem × 2 = 3rem internal but the dialog itself sits at the viewport edge with a 1rem visual gutter).
2. **Responsive grids:** unconditional `grid-cols-2`, `md:grid-cols-2`, and `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries inside dialog forms were normalized to `grid-cols-1 sm:grid-cols-2`. This drops the `useIsMobile()` hook dependency for layout (one fewer re-render trigger) and gives Tailwind the same breakpoint semantics the rest of the codebase already uses.
3. **Action button rows:** `flex justify-end gap-2` → `flex flex-col-reverse gap-2 sm:flex-row sm:justify-end` so Cancel / Save become full-width stacked buttons on mobile (Save sits visually first because reversed-flex), with the desktop right-aligned row preserved at sm+.

### Dialogs intentionally NOT touched (still deferred)

Per the < 1500 LoC budget Wave 28 left these in scope but not shipped:

| Dialog | File | Why not shipped now |
|---|---|---|
| Inspections | `client/src/pages/inspections.tsx` | Already uses `max-w-4xl max-h-[90vh] overflow-y-auto`. Functional at 375px but a future audit could clamp the wrapper for the floor case. Spotted, deferred. |
| Meetings (4 dialogs) | `client/src/pages/meetings.tsx` | All 4 already `max-w-lg max-h-[90vh] overflow-y-auto` — readable at 375px. Adding the clamp is a low-value follow-up, not a fix. Deferred. |
| Announcements | `client/src/pages/announcements.tsx` | `max-w-md` + `max-w-2xl` shells; functional. Deferred. |
| Communications composer | `client/src/pages/communications.tsx` | Multiple `max-w-2xl`/`max-w-lg` dialogs. Communications was extensively reworked in earlier waves — its dialog density is acceptable. Deferred. |
| Insurance / Inspections / Units / Board / Budgets / Utilities / Foundation | various | Each ships dialogs that already match the Wave 28 patterns (responsive grids, sensible max-w). Spot-checked during audit, no cramping observed. Deferred to future "round 3" follow-up if needed. |

The deferral is intentional — Wave 28 prioritized **dialogs that grep showed unconditional `grid-cols-2` or fixed widths > 380px without any viewport clamp**, plus the original target list.

### Patterns NOT applied (intentionally)

- **Sheet/Drawer migration on mobile:** the spec hints at swapping `<Dialog>` for `<Sheet>` (mobile drawer) at narrow widths. That's a structural rewrite that touches every dialog's open-state coordination, not a layout fix. Stays deferred.
- **Native HTML5 date pickers:** all date inputs already use `<Input type="date" />`, which delegates to the platform native picker on mobile. No change needed.
- **Form label shrinking:** the audited dialogs use Radix-driven labels at default sizing. Shrinking labels on mobile risks a11y regression (sub-12px text); we kept current sizing.

### Tests

- `tests/wave-28-form-dialog-density.client.test.tsx` ships **25 assertions**: 3 layout-shell scrollWidth proxies + 22 static-source guards (each of the 11 audited files must contain `max-w-[calc(100vw-2rem)]` AND must NOT contain the `isMobile ? "grid-cols-1" : "grid-cols-2"` ternary).

## Change Log

| Date | Author | Change |
|---|---|---|
| 2026-04-24 | Wave 14 | Initial SPEC LOCK. Module moves from QUEUED → SPEC LOCKED → IN BUILD (partial). |
| 2026-04-24 | Wave 18 | Round-2 mobile follow-ups: F1 Foundation card-fallback + F2 header compression + F4 320px floor. F3/F5 stay deferred. |
| 2026-04-24 | Wave 28 | Closes F5 form-dialog density. 14 dialogs across 10 files patched. |

## Wave 31 — Dialog round 2 (2026-04-25)

Wave 28 closed F5 against an initial set of 14 dialogs but explicitly
deferred a long tail of operator dialogs that were still missing the
viewport clamp (`max-w-[calc(100vw-2rem)]`) on small screens. Round 2
sweeps that tail.

Files patched (~33 dialogs, mechanical viewport-clamp + responsive grid +
responsive button row where the form had not already been migrated):

- `client/src/pages/board.tsx` (1 dialog — full sweep: viewport clamp,
  responsive grid, responsive action button row).
- `client/src/pages/board-packages.tsx` (1 dialog).
- `client/src/pages/inspections.tsx` (1 dialog).
- `client/src/pages/meetings.tsx` (5 dialogs).
- `client/src/pages/announcements.tsx` (2 dialogs).
- `client/src/pages/communications.tsx` (2 dialogs).
- `client/src/pages/insurance.tsx` (1 dialog).
- `client/src/pages/units.tsx` (1 dialog).
- `client/src/pages/community-hub.tsx` (1 dialog).
- `client/src/pages/financial-budgets.tsx` (3 dialogs).
- `client/src/pages/financial-foundation.tsx` (3 dialogs).
- `client/src/pages/financial-utilities.tsx` (1 dialog).
- `client/src/pages/financial-ledger.tsx` (2 dialogs).
- `client/src/pages/financial-delinquency.tsx` (1 dialog).
- `client/src/pages/financial-reconciliation.tsx` (3 dialogs).
- `client/src/pages/financial-invoices.tsx` (1 dialog).
- `client/src/pages/resident-feedback.tsx` (1 dialog).
- `client/src/pages/roadmap.tsx` (3 dialogs).

Approach: each previously bare `max-w-{lg,md,xl,2xl,3xl,4xl}` was rewritten
to `max-h-[90vh] max-w-[calc(100vw-2rem)] overflow-y-auto sm:max-h-[85vh]
sm:max-w-{X}` so the dialog clamps to a 16-px gutter on phones and snaps
back to its desktop max-width at the `sm` breakpoint.

### What's still deferred

- A handful of compact confirm dialogs (e.g. amenities-admin small
  destructive confirms, association-context picker) where the existing
  `max-w-sm` / `max-w-3xl` already fits a 360-px viewport with no
  horizontal scroll. These can stay un-clamped without harm.
- A focused dialog audit on the marketing / onboarding flows
  (`new-association.tsx`, `plan-signup.tsx`) that have their own
  spec — held for a future signup-flow polish wave.

| Date | Author | Change |
|---|---|---|
| 2026-04-25 | Wave 31 | Dialog round 2: ~33 more dialogs viewport-clamped across 18 files. |

## Wave 43 — Dialog round 3 (2026-04-26)

Wave 31's report deferred ~10 dialogs as "already use `max-w-lg`/`max-w-2xl`
shells with sensible internal grids — functional at 375px, not regressing."
Re-audit those: Wave 31 added the viewport clamp on every shell, but several
internal grids and action button rows were still using the JS-gated
`isMobile ? "grid-cols-1" : "grid-cols-2"` / `flex justify-end` ternary
pattern (one extra render-dependency on a hook, and won't adapt if the
viewport resizes mid-session without a re-render). Wave 43 normalizes
those to Tailwind responsive prefixes — the same canonical Wave 28
patterns applied to the deferred files.

### What changed (per file)

All fixes are layout-only — no validation, mutation, or form-state changes.

| # | File | Dialog(s) | Patterns applied |
|---|---|---|---|
| 1 | `client/src/pages/inspections.tsx` | Inspection create + finding card | Two `md:grid-cols-2` → `grid-cols-1 sm:grid-cols-2`. Action row → `flex flex-col-reverse gap-2 sm:flex-row sm:justify-end` + `w-full sm:w-auto` on Cancel/Create. |
| 2 | `client/src/pages/meetings.tsx` | Schedule Meeting + Reminder Rule | Three unconditional `grid-cols-2` → `grid-cols-1 sm:grid-cols-2` (meeting type/date pair, reminder trigger/recipients pair, reminder days/types pair). |
| 3 | `client/src/pages/insurance.tsx` | Add/Edit Policy | Three `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries normalized to `grid-cols-1 sm:grid-cols-2`. |
| 4 | `client/src/pages/communications.tsx` | Confirm Send + Emergency Push + Reminder Rule | Two bare `DialogContent` shells got the viewport clamp + `sm:max-w-lg`. Two `isMobile ? grid grid-cols-1 : flex justify-end` action rows + one unconditional `flex justify-end gap-2` row → reversed-stack pattern. |
| 5 | `client/src/pages/announcements.tsx` | Notify Push/SMS + Create/Edit Announcement | One `md:grid-cols-2` → `grid-cols-1 sm:grid-cols-2`. Two `flex justify-end gap-2` action rows → reversed-stack pattern. |
| 6 | `client/src/pages/financial-utilities.tsx` | Add Utility Payment | Three `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries normalized. |
| 7 | `client/src/pages/financial-invoices.tsx` | Create Invoice | Three `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries normalized. |
| 8 | `client/src/pages/financial-delinquency.tsx` | Threshold create/edit | Three `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries normalized + action row migrated to reversed-stack. |
| 9 | `client/src/pages/financial-ledger.tsx` | Ledger entry + Send notice | Two `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries normalized + the send-notice `DialogFooter` migrated to reversed-stack. |
| 10 | `client/src/pages/financial-budgets.tsx` | Create Budget | One `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternary (start/end date pair) normalized. The Reserve Fund card on the page (not a dialog) is intentionally not touched. |
| 11 | `client/src/pages/financial-foundation.tsx` | Approval request | Action row migrated to reversed-stack pattern. |
| 12 | `client/src/pages/financial-reconciliation.tsx` | New Period + Manual Match + Import | Two `${isMobile ? "grid-cols-1" : "grid-cols-2"}` ternaries inside the period and import dialogs normalized + all three action rows migrated to reversed-stack. |
| 13 | `client/src/pages/board-packages.tsx` | Template create/edit | Three `md:grid-cols-{2,3}` grids → `grid-cols-1 sm:grid-cols-{2,3}` + the action row migrated to reversed-stack (replaced the bespoke `${isMobile ? "flex-col" : ""}` swap). |
| 14 | `client/src/pages/community-hub.tsx` | Add Quick Action + Add Info Block + Add Map Layer | Three bare `DialogContent` shells got the viewport clamp + `sm:max-w-lg`. |
| 15 | `client/src/pages/resident-feedback.tsx` | Note dialog | Action row migrated to reversed-stack pattern. |

That's **15 files / 23 dialogs** touched (a few files host multiple form
dialogs that all needed the same treatment). All changes are pure
Tailwind class normalization — zero behavioral changes.

### Patterns applied (same as Wave 28)

1. **Viewport clamp** on the few `DialogContent`s that Wave 31 missed
   (`community-hub.tsx`, the two Wave 31-skipped dialogs in
   `communications.tsx`): `max-w-[calc(100vw-2rem)] sm:max-w-{lg|xl|2xl|3xl}`.
2. **Responsive grids:** every `${isMobile ? "grid-cols-1" : "grid-cols-2"}`
   ternary inside a dialog form normalized to
   `grid-cols-1 sm:grid-cols-2` (and likewise for the few `md:grid-cols-2`
   instances that were inside dialogs — they collapse on `sm` now).
3. **Action button rows:** every dialog action row is now
   `flex flex-col-reverse gap-2 sm:flex-row sm:justify-end` with
   `w-full sm:w-auto` on each button, so on phones the primary action
   sits visually first as a full-width button (reversed-flex) and the
   desktop right-aligned row is preserved at `sm:`+.

### Dialogs intentionally skipped (and why)

| File | Why skipped |
|---|---|
| `client/src/pages/units.tsx` | The two main grids (building details L760, unit details L826) already use `grid-cols-1 gap-4 sm:grid-cols-2`. The remaining `${isMobile ? "grid grid-cols-1" : "flex"}` rows are bespoke multi-input owner-add and inline-new-person editor rows where each child has its own `flex-1 h-8` vs `min-h-11` size class — converting them to `flex-col sm:flex-row` would also have to coordinate the per-input width classes, which crosses into structural form-logic change. The original dialog already renders cleanly at 375px in the existing isMobile branch. |
| `client/src/pages/board.tsx` | Wave 31 already shipped the full sweep here (viewport clamp + `grid-cols-1 sm:grid-cols-2` + `flex flex-col-reverse sm:flex-row` action row). The lone `grid-cols-2` at line 304 is inside the row card display (not a dialog), so it stays. |
| `client/src/pages/financial-budgets.tsx` Reserve Fund card | The `md:grid-cols-2` at line 606 is a stat-card grid on the main page, not a dialog form. The four cards inside it (Reserve Baseline, Annual Contributions, Annual Reserve Expenses, Annual Special Assessments) are short numeric tiles that fit two-up at 375px. Out of scope for "form-dialog mobile fixes". |
| `client/src/pages/financial-budgets.tsx` Version + Line dialogs | Already simple stacked single-column forms with a full-width Save button. No grid-cols-2 inside, no action row regression. |
| `client/src/pages/communications.tsx` main composer pages | The big `grid grid-cols-1 md:grid-cols-3 gap-3` panels at L1064 / L1113 / L1424 / L1481 are the page-level filter panels that wrap the dialog triggers — they're already responsive (`md:` collapses to single-column below 768px). The audit only touches the two dialogs Wave 31 missed (Confirm Send + Reminder Rule) plus the unconditional flex-justify-end action row in Emergency Push. |
| `client/src/pages/roadmap.tsx` | The four `grid grid-cols-2 gap-3` blocks at L1354 / L1387 / L1408 / L1424 are inside an admin-only roadmap detail dialog that's already viewport-clamped (Wave 31). Each block is two short numeric tiles (e.g. "stage / status" pairs) that fit two-up at 375px. Touching them would cost more LoC than the readability win. Stays. |
| `client/src/pages/community-hub.tsx` info-block stat grids (L388, L428, L702, L1039, L1067) | Already use `sm:grid-cols-2` (mobile-first). No work needed. |

### Dialogs explicitly NOT in scope

The `marketing/onboarding` flows (`new-association.tsx`,
`plan-signup.tsx`) called out in Wave 31 stay deferred — they own their
own spec (4.4 founder-ratification path) and a polish wave will close
them in concert with the broader signup flow review.

### Tests

- `tests/wave-43-form-dialog-density.client.test.tsx` ships **18
  assertions**: 2 layout-shell scrollWidth proxies (Inspection +
  Delinquency Threshold) + 16 static-source guards (each of 8 newly
  audited files must contain `max-w-[calc(100vw-2rem)]` AND must NOT
  contain the legacy `isMobile ? "grid-cols-1" : "grid-cols-2"`
  ternary).

| Date | Author | Change |
|---|---|---|
| 2026-04-26 | Wave 43 | Dialog round 3: 23 dialogs across 15 files normalized to canonical Wave 28 patterns. Closes Wave 31's deferral list. |
