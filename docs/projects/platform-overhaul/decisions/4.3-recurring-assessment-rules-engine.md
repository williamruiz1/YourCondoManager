# 4.3 — Recurring assessment rules engine
**Status:** SKELETON — all resolutions PENDING
**Layer:** 4
**Type:** Decision
**Dependencies:** 0.2 (LOCKED — six-persona model) · 1.1 (LOCKED — Financials zone) · 1.2 (LOCKED — `/app/financials` hub) · 2.1 (LOCKED — role model) · 2.2 (LOCKED — Owner Portal access boundaries) · 2.3 (LOCKED — permission boundary corrections) · 3.2 (LOCKED — `/app/financial/*` route tree preserved)
**Owner:** YCM Command Center
**Last updated:** 2026-04-16

---

## Context

The Financials zone (1.1 Q3: "All money-related pages. Budgets, assessments, owner ledgers, payment tracking, financial reports.") contains three existing assessment-related subsystems that operate independently:

1. **HOA fee schedules** (`shared/schema.ts:248-261`, `hoaFeeSchedules` table) — association-level fee definitions with frequency, amount, grace days, active flag, but **no execution engine**. Fee schedules are read by the portal financial dashboard (`server/routes.ts:11358-11365`) but never automatically posted to ledgers.

2. **Recurring charge schedules** (`shared/schema.ts:575-613`, `recurringChargeSchedules` + `recurringChargeRuns` tables) — the only subsystem with an automated execution engine. Schedules define a `chargeDescription`, `amount`, `frequency` (monthly/quarterly/annual), `dayOfMonth`, and optional unit scope. A batch sweep in `server/index.ts:109-168` (`runDueRecurringCharges`) runs as part of the automation sweep (`server/index.ts:255-270`, `runAutomationSweep`) and posts `ownerLedgerEntries` for each active schedule whose `nextRunDate <= now`. Retry tracking exists via `recurringChargeRuns` with status enum `pending | success | failed | skipped | retrying` (`shared/schema.ts:596-613`).

3. **Special assessments** (`shared/schema.ts:263-277`, `specialAssessments` table) — one-time or installment-based assessments with `totalAmount`, `installmentCount`, `startDate`, `endDate`, per-unit exclusions, and an `autoPostEnabled` flag. Execution is handled by `server/assessment-installments.ts:60-186` (`processSpecialAssessmentInstallments`), which posts installment ledger entries with `referenceType: "special_assessment_installment"`. Also runs in the automation sweep (`server/index.ts:261`).

These three subsystems share no common rule model, no unified execution pipeline, and no consolidated UI. The operator manages them across three different surfaces:

- **Fee schedules**: `client/src/pages/financial-foundation.tsx` (Foundation tab, alongside chart-of-accounts and categories — `client/src/pages/financial-foundation.tsx:27`)
- **Recurring charges**: `client/src/pages/financial-recurring-charges.tsx` (embedded in Foundation page via `FinancialRecurringChargesContent` — `client/src/pages/financial-foundation.tsx:27`)
- **Special assessments**: `client/src/pages/financial-assessments.tsx` (Assessments tab inside Billing page — `client/src/pages/financial-billing.tsx:5,23,30-31`)

### Downstream integration points

- **Ledger**: All three subsystems ultimately post to `ownerLedgerEntries` (`shared/schema.ts:536-548`) with `entryType` values `charge` or `assessment`.
- **Delinquency**: Delinquency tracking (`shared/schema.ts:2095-2131`, `delinquencyThresholds` + `delinquencyEscalations`) and delinquency notices (`shared/schema.ts:2961-3007`, `delinquencySettings` + `delinquencyNotices`) consume ledger balances. The delinquency notice sweep runs in the same automation batch (`server/index.ts:264`).
- **Late fees**: Late fee rules (`shared/schema.ts:279-284`, `lateFeeRules`) apply to overdue balances and are managed on the Billing page's Late Fees tab (`client/src/pages/financial-billing.tsx:33-34`).
- **Owner Portal**: The portal financial dashboard (`server/routes.ts:11350-11397`) returns `feeSchedules` (from `recurringChargeSchedules`), `nextDueDate`, `balance`, `paymentPlan`, and `recentEntries` to the owner. Owners see their assessment schedule read-only in the Financials tab (`client/src/pages/owner-portal.tsx:2265`).
- **Autopay**: Owner autopay enrollment (`shared/schema.ts:1519`) triggers payment against posted ledger charges.

### Persona access (from 0.2 boundary matrix)

| Persona | `/app/financial/*` | Rule authoring | Rule viewing |
|---|---|---|---|
| Manager | Full | Full CRUD | Full |
| Board Officer | Full | Full CRUD | Full |
| Assisted Board | Read-only (PM toggle can expand) | None (default) | Read-only |
| PM Assistant | Full (PM toggle can restrict) | Full (default) | Full (default) |
| Owner | Portal only | None | Own schedule only (`/api/portal/financial-dashboard`) |
| Platform Admin | Not customer-tenant scope | None | None |

---

## Conflict Analysis

Six decision axes emerge from the fragmented state:

1. **Three rule models, no unification.** `hoaFeeSchedules`, `recurringChargeSchedules`, and `specialAssessments` define overlapping but incompatible rule shapes. A recurring monthly assessment can be modeled in either fee schedules (no execution) or recurring charges (with execution). Resolved in **Q1**.

2. **Fee schedules have no execution engine.** `hoaFeeSchedules` rows are read by the portal dashboard but never posted to ledgers. Their `frequency` and `amount` fields imply recurring behavior that does not actually occur. Resolved in **Q2**.

3. **Execution is split across two code paths.** `runDueRecurringCharges` (`server/index.ts:109`) and `processSpecialAssessmentInstallments` (`server/assessment-installments.ts:60`) run independently in the same automation sweep, each with its own retry/skip logic. Resolved in **Q3**.

4. **No delinquency trigger from rule execution.** Recurring charge runs and assessment installment posting do not directly trigger delinquency evaluation. Delinquency detection relies on a separate sweep reading ledger balances. The gap between charge posting and delinquency detection is undefined. Resolved in **Q4**.

5. **Owner Portal shows `recurringChargeSchedules` but not `hoaFeeSchedules` or `specialAssessments`.** The portal financial dashboard (`server/routes.ts:11358-11365`) queries only `recurringChargeSchedules` for `feeSchedules`. Owners cannot see HOA fee schedule definitions or upcoming special assessment installments. Resolved in **Q5**.

6. **PM Toggle implications for Assisted Board rule visibility.** 0.2 grants Assisted Board read-only access to Financials by default. The PM can expand this via toggle, but there is no specification for what "read-only" means in the context of assessment rules — can Assisted Board see rule definitions, execution history, or just ledger outcomes? Resolved in **Q6**.

---

## Open Questions

### Q1 — Should the three rule models (fee schedules, recurring charges, special assessments) be unified into a single assessment rules engine, or remain separate subsystems?

**PENDING — resolution required.**

Current state:
- `hoaFeeSchedules` (`shared/schema.ts:248-261`): `name`, `amount`, `frequency` (monthly/quarterly/annually/one-time), `startDate`, `endDate`, `graceDays`, `isActive`
- `recurringChargeSchedules` (`shared/schema.ts:575-590`): `chargeDescription`, `amount`, `frequency` (monthly/quarterly/annual), `dayOfMonth`, `nextRunDate`, `status` (active/paused/archived), `unitId` (nullable = all units), `maxRetries`
- `specialAssessments` (`shared/schema.ts:263-277`): `name`, `totalAmount`, `installmentCount`, `startDate`, `endDate`, `autoPostEnabled`, `excludedUnitIdsJson`

Hypothesis A — **Unify**: Create a single `assessmentRules` table that generalizes all three. Rule type enum: `recurring_dues | special_assessment | one_time_fee`. Shared fields: frequency, amount model, unit scope, effective dates, execution configuration. Migrate existing data.

Hypothesis B — **Federate**: Keep the three tables. Add a shared `assessmentRuleRegistry` view or materialized index that presents a unified read surface for the UI and portal, but leave execution engines separate.

Hypothesis C — **Retire fee schedules, keep two**: Migrate `hoaFeeSchedules` into `recurringChargeSchedules` (adding execution capability). Keep `specialAssessments` separate because its installment model is structurally different.

**Acceptance Criteria:** TBD after resolution.

---

### Q2 — Should `hoaFeeSchedules` gain an execution engine, or should its data be migrated into `recurringChargeSchedules`?

**PENDING — resolution required.**

Current state: `hoaFeeSchedules` rows are stored and served to the portal dashboard via `recurringChargeSchedules` (the portal actually queries `recurringChargeSchedules`, not `hoaFeeSchedules` — `server/routes.ts:11359`). Fee schedules are effectively documentation-only records with no automated posting behavior.

Hypothesis A — **Add execution to fee schedules**: Extend `hoaFeeSchedules` with `nextRunDate`, `status`, and run-tracking columns. Build a parallel execution sweep.

Hypothesis B — **Migrate to recurring charges**: Map each active `hoaFeeSchedules` row into a `recurringChargeSchedules` row with matching frequency, amount, and dates. Deprecate the `hoaFeeSchedules` table. Update the fee-schedule CRUD endpoints (`server/routes.ts:2855-2877`) to write to `recurringChargeSchedules`.

Hypothesis C — **Leave as-is**: Fee schedules remain informational. Only `recurringChargeSchedules` executes. Document the distinction explicitly in the UI.

**Acceptance Criteria:** TBD after resolution.

---

### Q3 — Should the recurring charge execution (`server/index.ts:109-168`) and special assessment installment posting (`server/assessment-installments.ts:60-186`) be consolidated into a single execution pipeline?

**PENDING — resolution required.**

Current state: Both run independently in `runAutomationSweep` (`server/index.ts:256`). They share the same target table (`ownerLedgerEntries`) but use different:
- Selection logic (recurring: `nextRunDate <= now` + `status: active`; assessment: `isActive + autoPostEnabled` with installment-number math)
- Run tracking (recurring: `recurringChargeRuns` table with retry/status; assessment: idempotency via `referenceId` dedup set, no run-log table)
- Error handling (recurring: per-unit run record with `failed`/`retrying` status; assessment: silent skip)

Hypothesis A — **Single pipeline**: Create a unified `assessmentExecution` service that dispatches to type-specific handlers but shares selection, run-logging, retry, and error reporting infrastructure.

Hypothesis B — **Keep separate, add shared run log**: Keep the two execution functions. Add a shared `assessmentRunLog` table that both write to, giving operators a single audit trail.

Hypothesis C — **Keep separate, no change**: Accept the current dual-path architecture. The two subsystems are structurally different enough that consolidation adds complexity without proportional benefit.

**Acceptance Criteria:** TBD after resolution.

---

### Q4 — How should rule execution trigger delinquency evaluation?

**PENDING — resolution required.**

Current state: Delinquency detection is decoupled from charge posting. The delinquency notice sweep (`server/index.ts:264`, `runAllDelinquencyNotices`) runs in the same automation batch as charge posting but reads ledger balances independently. Delinquency thresholds (`shared/schema.ts:2095-2108`) define stage-based escalation by `minimumBalance` and `minimumDaysOverdue`. Delinquency settings (`shared/schema.ts:2961-2975`) configure `gracePeriodDays`, `bucketBoundariesJson` (`[30,60,90]` default), and `autoLateFeeEnabled`.

The gap: when a recurring charge posts a new ledger entry, the delinquency system does not immediately re-evaluate that owner's status. Re-evaluation happens only on the next automation sweep cycle.

Hypothesis A — **Synchronous trigger**: After each ledger entry is posted by the rules engine, immediately invoke a lightweight delinquency check for that owner/unit. Escalate or de-escalate in the same transaction.

Hypothesis B — **Event-based**: Emit a `ledger.entry.posted` event after charge posting. A delinquency subscriber processes these events asynchronously but within the same sweep cycle, after all charges are posted.

Hypothesis C — **Status quo (sweep-based)**: Accept the current model where delinquency detection runs as a peer job in the automation sweep. The latency between charge posting and delinquency detection is bounded by the sweep interval.

**Acceptance Criteria:** TBD after resolution.

---

### Q5 — What assessment schedule information should the Owner Portal display?

**PENDING — resolution required.**

Current state: The portal financial dashboard (`server/routes.ts:11350-11397`) returns:
- `feeSchedules`: derived from `recurringChargeSchedules` only (`server/routes.ts:11385`) — `hoaFeeSchedules` and `specialAssessments` are not included
- `nextDueDate`: earliest `nextRunDate` from active recurring charge schedules (`server/routes.ts:11377-11380`)
- `paymentPlan`: active payment plan if any (`server/routes.ts:11387-11395`)
- `recentEntries`: last 10 ledger entries (`server/routes.ts:11396`)

Hypothesis A — **Unified schedule view**: Show all applicable rules (recurring dues, special assessments, fee schedules) in a single "My Assessment Schedule" section. Include upcoming amounts, due dates, and installment progress for special assessments.

Hypothesis B — **Recurring only + special assessment installments**: Extend the portal dashboard to include special assessment installment schedule (next installment number, amount, due date) alongside recurring charge schedules. Leave `hoaFeeSchedules` out (since they have no execution, they have no "next due date").

Hypothesis C — **Keep current**: Only show `recurringChargeSchedules` data. Owners learn about other assessments through posted ledger entries and notices.

**Acceptance Criteria:** TBD after resolution.

---

### Q6 — What does "read-only Financials" mean for Assisted Board in the context of assessment rules?

**PENDING — resolution required.**

0.2 PM-Managed Default Access Table (`docs/projects/platform-overhaul/decisions/0.2-persona-map.md:117-131`) grants Assisted Board `View: Yes, Write: No` for "Financials — reports & statements" and `View: Yes, Write: Approve action only` for "Budget — annual approval". Assessment rules are not explicitly listed.

Hypothesis A — **Full rule visibility, no write**: Assisted Board can view all rule definitions (fee schedules, recurring charges, special assessments), execution history, and run logs. They cannot create, edit, pause, or delete rules. PM toggle can expand to write access.

Hypothesis B — **Outcome-only visibility**: Assisted Board sees only ledger entries and financial reports that result from rule execution. Rule definitions and execution history are Manager/Board Officer surfaces only. PM toggle can expand to rule visibility.

Hypothesis C — **Tiered**: Assisted Board sees rule definitions (name, amount, frequency) but not execution history or run logs. PM toggle can expand to full visibility.

**Acceptance Criteria:** TBD after resolution.

---

### Q7 — What is the rule definition model for the unified/consolidated engine?

**PENDING — resolution required. Depends on Q1 resolution.**

Regardless of whether Q1 selects unification, federation, or partial migration, the rules engine needs a canonical data model for rule authoring. This question covers:

- **Frequency options**: Current enums differ — `hoaFeeSchedules` supports `monthly | quarterly | annually | one-time` (`shared/schema.ts:247`); `recurringChargeSchedules` supports `monthly | quarterly | annual` (`shared/schema.ts:573`). Should these be harmonized? Should `semi-annual`, `weekly`, or custom cron expressions be added?
- **Amount model**: Flat amount per unit (current) vs. percentage of unit assessed value vs. tiered by unit type/size vs. custom formula. Current `hoaFeeSchedules.amount` and `recurringChargeSchedules.amount` are both flat `real` values (`shared/schema.ts:252,581`).
- **Unit scope**: `recurringChargeSchedules.unitId` (`shared/schema.ts:578`) is nullable (null = all units). `specialAssessments.excludedUnitIdsJson` (`shared/schema.ts:274`) uses an exclusion list. Should the model support inclusion lists, unit-type filters, or building-level scope?
- **Effective dates**: `hoaFeeSchedules` has `startDate` + `endDate` (`shared/schema.ts:254-255`). `recurringChargeSchedules` has `nextRunDate` but no explicit end date. `specialAssessments` has `startDate` + `endDate` (`shared/schema.ts:268-269`). Should all rules support effective date ranges with explicit start/end?
- **Grace period**: Only `hoaFeeSchedules` has `graceDays` (`shared/schema.ts:256`). Should grace periods be a universal rule attribute that feeds into late fee and delinquency evaluation?

**Acceptance Criteria:** TBD after resolution.

---

### Q8 — Should rule execution be batch-only (automation sweep) or also support on-demand triggering?

**PENDING — resolution required.**

Current state:
- Recurring charges: batch sweep via `runDueRecurringCharges` (`server/index.ts:109`) in `runAutomationSweep` (`server/index.ts:255`), **plus** on-demand via `POST /api/financial/recurring-charges/run` (`server/routes.ts:3572`).
- Special assessments: batch sweep via `runAutomaticSpecialAssessmentInstallments` (`server/assessment-installments.ts:188`) in `runAutomationSweep`, **plus** on-demand via `POST /api/financial/assessments/run` (`server/routes.ts:2959`).

Hypothesis A — **Both**: Retain both batch and on-demand. Batch handles scheduled execution. On-demand allows Manager to trigger a dry-run or immediate posting for a specific rule or association. Unify the on-demand trigger into a single endpoint.

Hypothesis B — **Batch only**: Remove on-demand triggers. All execution happens through the automation sweep. Managers can adjust `nextRunDate` to force immediate execution on the next sweep.

Hypothesis C — **Keep current dual endpoints**: No change to the existing per-subsystem on-demand triggers. Each subsystem retains its own manual-run endpoint.

**Acceptance Criteria:** TBD after resolution.

---

### Q9 — Where does the consolidated assessment rules UI live in the route tree?

**PENDING — resolution required.**

Current state: Rule management is scattered:
- Fee schedules tab in Foundation page (`/app/financial/foundation` — `client/src/pages/financial-foundation.tsx`)
- Recurring charges embedded in Foundation page (`client/src/pages/financial-recurring-charges.tsx`, imported at `client/src/pages/financial-foundation.tsx:27`)
- Special assessments tab in Billing page (`/app/financial/billing` — `client/src/pages/financial-billing.tsx:23,30`)

3.2 (LOCKED) preserves all `/app/financial/*` routes (`3.2-route-restructure.md:238-251`). The Financials hub at `/app/financials` is new (3.2 Q1).

Hypothesis A — **New sub-page**: Add `/app/financial/assessments-rules` (or `/app/financial/rules`) as a dedicated rule management surface linked from the Financials hub. Consolidates fee schedules, recurring charges, and special assessments into one page.

Hypothesis B — **Expand Billing page**: Add an "Assessment Rules" tab to the existing Billing page (`/app/financial/billing`). Keep the Assessments tab for special assessment management and add a new tab for recurring rules.

Hypothesis C — **Keep scattered, link from hub**: Leave fee schedules in Foundation, recurring charges in Foundation, and special assessments in Billing. The Financials hub links to all three. No consolidation.

**Acceptance Criteria:** TBD after resolution.

---

## Selected Resolution

### Q1 — Unified rule model vs federated vs partial migration
**RESOLVED — 2026-04-21:** Retire `hoaFeeSchedules` → migrate into `recurringChargeSchedules`. Keep `specialAssessments` separate because its installment model is structurally different. See Q1 original lock entry in Decision Log.

### Q2 — `hoaFeeSchedules` execution engine vs migration
**RESOLVED — 2026-04-21:** Migrate `hoaFeeSchedules` rows into `recurringChargeSchedules`; deprecate the table. See Q2 original lock entry in Decision Log.

### Q3 — Consolidate execution pipelines?
**RESOLVED — 2026-04-24 — Hypothesis A (unified pipeline).** Founder decision: optimize for long-term correctness, not short-term risk avoidance. Consolidate recurring-charge execution (`server/index.ts:109-168`) and special-assessment installment posting (`server/assessment-installments.ts:60-186`) into a single `assessmentExecution` service.

**Architecture:**
- New `server/assessment-execution.ts` hosts the unified orchestrator. Dispatches to rule-type-specific handlers via an internal registry keyed by `ruleType`.
- Shared infrastructure: selection query (eligible rules for this sweep window), idempotency (via `ruleType + ruleId + unitId + dueDate` composite key), retry policy, run-log writes, error aggregation, observability counters.
- Rule-type handlers remain the authoritative semantics for each rule class: `recurringCharges.post()` and `specialAssessments.postInstallment()` are refactored to be pure handlers that take a `RuleExecutionContext` and return a `RuleExecutionOutcome`. No direct `ownerLedgerEntries.insert` from handler code — the orchestrator owns the ledger write.
- New `assessmentRunLog` table is the canonical audit trail (composite of what Hypothesis B would have been — subsumed by the unified pipeline). Columns: `id, associationId, ruleType, ruleId, unitId, runStartedAt, runCompletedAt, status (success|failed|retrying|skipped|deferred), amount, ledgerEntryId (nullable), errorCode (nullable), errorMessage (nullable), retryAttempt`. Both rule types now write here; no silent failures.
- Feature-flagged rollout: `ASSESSMENT_EXECUTION_UNIFIED` (default OFF). Phase 1 wires the new orchestrator behind the flag, mirrors writes to both old and new paths for a parity window (shadow-write). Phase 2 flips the flag ON per-association under Manager control; parity diff surfaces as an ops alert. Phase 3 retires the legacy functions and the shadow-write path.

**Rationale for A over B (shared run-log only):** B is cheaper but leaves two execution engines with different retry/selection/error semantics. Every future rule-type addition (Q7 harmonization, any new rule class) compounds the divergence. A is more work now but produces one pipeline that future rule types plug into cleanly. Founder directive: right-for-the-long-term.

**Acceptance Criteria:**
- `server/assessment-execution.ts` exists with a registered handler set covering recurring charges + special-assessment installments; orchestrator owns ledger writes and audit writes.
- `assessmentRunLog` table migration shipped; both rule types write to it (verified by integration test).
- `ASSESSMENT_EXECUTION_UNIFIED` flag introduced in `shared/feature-flags.ts`; defaults OFF; per-association override supported.
- Shadow-write parity test: with the flag OFF, the new orchestrator runs alongside legacy in dry-run mode and asserts identical `ownerLedgerEntries` + `assessmentRunLog` would-be rows for a representative sweep window.
- Ops runbook documents flag-flip procedure, parity-drift alert, rollback lever.
- Legacy functions (`runDueRecurringCharges`, `processSpecialAssessmentInstallments`) retained but unreachable when flag is ON; marked `@deprecated` with planned removal in 5.1 cleanup.

### Q4 — Delinquency trigger coupling
**RESOLVED — 2026-04-21:** Status-quo sweep-based delinquency preserved. See Q4 original lock entry in Decision Log.

### Q5 — Owner Portal assessment schedule visibility
**RESOLVED — 2026-04-24 — Hypothesis B (recurring + special-assessment installments) + owner-facing assessment detail surface.**

Owner Portal financial dashboard extends to show:

1. **Recurring schedule summary.** Active `recurringChargeSchedules` (post-Q2 migration, includes everything that was in `hoaFeeSchedules`). Next due date, amount, frequency per unit.
2. **Upcoming special-assessment installments.** For each active `specialAssessments` row affecting the owner's unit: installment number + amount + due date + remaining installments.
3. **Assessment detail surface (per-special-assessment drill-in).** Owner clicks an assessment and sees: label (`specialAssessments.name`), total principal (`totalAmount`), **interest rate** (new field), **term** (new field), **allocation method** (new field — how owner portion is derived), **owner's portion** (derived from allocation method × unit attribute), **payment options** (lump-sum payoff vs installment plan), **total interest to be paid** (derived), installments remaining, history of posted installments for this owner with ledger links.
4. **`hoaFeeSchedules` NOT shown.** Retired in Q1/Q2; data migrated to recurring charges.

**Schema extensions required** (spec'd here, built with 4.3 engine PR or as a precursor):
- `specialAssessments.interestRatePercent: real` (nullable; null = no interest / flat principal split).
- `specialAssessments.termMonths: integer` (nullable; when present, drives amortization calc — may differ from `installmentCount` if installments are non-monthly).
- `specialAssessments.allocationMethod: pgEnum("assessment_allocation_method", ["per-unit-equal", "per-sq-ft", "per-ownership-share", "custom"])` — default `per-unit-equal` for backward compatibility.
- `specialAssessments.allocationCustomJson: jsonb` — populated only when `allocationMethod = "custom"`; `{ [unitId]: portionPercent }` map.
- `specialAssessments.paymentOptionsJson: jsonb` — `{ lumpSumAllowed: boolean, lumpSumDiscountPercent: number|null, customInstallmentPlansAllowed: boolean }`.
- Owner-portion calc lives in `server/assessment-ownership.ts` (new module): given `(assessment, unit)` returns `{ principal, interest, total, installmentAmount, remainingInstallments }`. Pure function, unit-testable.
- Portal endpoint `GET /api/portal/assessments/:assessmentId/detail` (new) returns the derived per-owner view. Owner-portal-scoped (not cross-owner visible).

**Acceptance Criteria:**
- Portal dashboard shows recurring + installment summaries for the authenticated owner's unit.
- Per-assessment drill-in page renders the nine fields above with live-calculated owner portion.
- Allocation calc has ≥ 90% unit-test coverage including edge cases (per-sq-ft with missing `squareFootage`, per-ownership-share with missing `ownershipPercent`, custom allocation summing ≠ 100%).
- Payment-options JSON is read-only in portal (managed by Manager via rule CRUD).
- Historical assessments (pre-migration, missing new fields) default to `allocationMethod = "per-unit-equal"`, `interestRatePercent = null`, showing owner portion as `totalAmount / activeUnitCount`.

### Q6 — Assisted Board read-only Financials semantics
**RESOLVED — 2026-04-24 — Hypothesis A (full rule visibility, no write).**

Assisted Board sees all rule definitions (fee schedules — pre-migration / recurring charges — post-migration / special assessments), full execution history (`assessmentRunLog`), and run logs. They cannot create, edit, pause, or delete rules. PM toggle can expand to write access per 0.2's PM-Managed Default Access Table + PM Toggle Configuration Model.

**Rationale:** Assisted Board is an oversight role. Hiding rule definitions and run history defeats the role's purpose — oversight requires auditability. Execution history is the audit trail that allows a board to confirm the PM is posting charges correctly.

**Acceptance Criteria:**
- Assisted Board (default, no PM toggle) can GET all rule definitions and run-log rows scoped to their association(s).
- Assisted Board write attempts on rule CRUD endpoints return 403 with a structured error code (`assisted_board_read_only`).
- PM toggle `assessment_rules_write` (new toggle key) expands to write access when set; respected by CRUD endpoints and UI gating.
- Portal parity: Assisted Board does not appear in `/portal` personas; this resolution scopes to `/app` operator surfaces.

### Q7 — Canonical rule-definition model
**RESOLVED — 2026-04-24 — bundled proposal accepted.**

- **Frequency:** harmonize to `monthly | quarterly | annually | semi-annually | one-time`. No custom cron expressions (operational risk, low demand). `semi-annually` added because it's a real condo cadence. New pgEnum `assessmentFrequencyEnum`; both legacy enums (`feeFrequencyEnum`, `recurringChargeFrequencyEnum`) migrate to it.
- **Amount model:** retain flat-amount for now. Per-unit-type / per-sq-ft / tiered amounts are legitimate future demand but too large for this lock — filed as follow-up "4.x Amount Model Evolution" spec.
- **Unit scope:** support inclusion list + exclusion list + unit-type filter in addition to the existing `unitId` nullable (null = all). New columns: `unitScopeMode: pgEnum("assessment_unit_scope_mode", ["all-units", "inclusion-list", "exclusion-list", "unit-type-filter"])`, `includedUnitIdsJson: jsonb`, `unitTypeFilter: text` (nullable). The existing `excludedUnitIdsJson` on `specialAssessments` maps into the `exclusion-list` mode.
- **Effective dates:** universal `startDate` + optional `endDate` on all rule types (add to `recurringChargeSchedules`; retain on `specialAssessments`).
- **Grace period:** universal `graceDays` attribute on all rule types. Add to `recurringChargeSchedules` (currently only on `hoaFeeSchedules` → migrated); retain on fee schedules pre-migration. Feeds late-fee and delinquency evaluation.

**Acceptance Criteria:**
- `assessmentFrequencyEnum` exists; legacy enums retired via migration; data backfill preserves semantics (`annual` → `annually`; no existing rows use semi-annually so zero-touch backfill for that value).
- `unitScopeMode` + the three companion columns shipped; existing rows backfilled: `recurringChargeSchedules.unitId = null` → mode `all-units`; `recurringChargeSchedules.unitId = X` → mode `inclusion-list` with `[X]`; `specialAssessments.excludedUnitIdsJson = []` → mode `all-units`; non-empty → mode `exclusion-list`.
- `graceDays` present on `recurringChargeSchedules`; default `0`; feeds late-fee calc.
- Migration notes call out that `ownerLedgerEntries.dueDate` calc must add `graceDays` before late-fee evaluation (late-fee rule reads `graceDays` from the source rule, not from a global association setting).

### Q8 — Batch + on-demand, unified endpoint
**RESOLVED — 2026-04-24 — Hypothesis A.**

Retain both batch (automation sweep) and on-demand triggers. Unify the on-demand endpoint to `POST /api/financial/rules/:ruleId/run` which routes through the same `assessmentExecution` orchestrator as the sweep. Per-subsystem legacy endpoints (`/api/financial/recurring-charges/run`, `/api/financial/assessments/run`) become thin shims pointing to the unified endpoint and are retired in 5.1 cleanup.

**Acceptance Criteria:**
- `POST /api/financial/rules/:ruleId/run` exists; dispatches to the same handler set as the sweep.
- Optional body `{ dryRun: true }` returns projected outcomes without writing `ownerLedgerEntries` or `assessmentRunLog`.
- Legacy endpoints continue to function (shim) with deprecation headers; scheduled for removal in 5.1.
- Manager + Board Officer can call on-demand; Assisted Board gets 403 (unless PM toggle expansion). PM Assistant gets 403.

### Q9 — Consolidated rules UI placement
**RESOLVED — 2026-04-24 — Hypothesis A (new dedicated sub-page).**

New surface at `/app/financial/rules` (name: **Assessment Rules**). Linked from the Financials hub. Consolidates what was in Foundation (fee schedules, recurring charges) and Billing (special assessments) into a single rules-management page with three tabs: **Recurring** (post-Q2 contains migrated fee schedules + recurring charges), **Special Assessments**, **Run History** (unified `assessmentRunLog` view — filters by rule type + status + date window).

Legacy surfaces in Foundation + Billing retain a thin "Managed at Assessment Rules →" notice with a link for one release cycle, then removed in 5.1.

**Acceptance Criteria:**
- New route `/app/financial/rules` added to 3.2 amendment route table.
- Page gated by RouteGuard with `manager | board-officer | pm-assistant (write)` / `assisted-board | viewer (read-only)` access.
- Three-tab structure; Run History tab queries `assessmentRunLog` with filters.
- Foundation + Billing pages keep a banner linking to the new surface for one release cycle; banner removed in 5.1.
- Existing CRUD functionality preserved (no feature regression); only the placement changes.

---

## Acceptance Criteria (aggregate)

See per-Q AC above. Summary:

- New `assessmentExecution` service + `assessmentRunLog` table + `ASSESSMENT_EXECUTION_UNIFIED` feature flag (Q3).
- Schema extensions on `specialAssessments` for loan-style detail (Q5): `interestRatePercent`, `termMonths`, `allocationMethod`, `allocationCustomJson`, `paymentOptionsJson`.
- New `server/assessment-ownership.ts` module for per-owner portion calcs (Q5).
- New `GET /api/portal/assessments/:assessmentId/detail` endpoint (Q5).
- Owner Portal financial dashboard + drill-in surface (Q5).
- `assessmentFrequencyEnum` harmonization + enum migration (Q7).
- Unit scope mode columns + backfill (Q7).
- Universal `graceDays` + `startDate`/`endDate` on all rule types (Q7).
- Unified `POST /api/financial/rules/:ruleId/run` endpoint (Q8).
- New `/app/financial/rules` page with three tabs (Q9).
- `assessment_rules_write` PM toggle (Q6).
- Foundation + Billing pages get legacy-banner links for one release cycle (Q9).

Build scope is substantial (multi-PR). Sequencing: (1) schema extensions + flag, (2) unified orchestrator + run log + shadow-write, (3) Q5 portal surface, (4) Q9 UI page, (5) Q8 unified endpoint + legacy shims, (6) Q6 PM toggle, (7) 5.1 cleanup.

---

## How This Informs Downstream Layers

### Layer 4 — 4.2 (Owner Portal gaps)
Q5's resolution determines whether the Owner Portal financial dashboard is extended to show unified assessment schedules. Any portal-side changes are co-requisite with 4.2 Owner Portal gap work and the deferred `/portal` route restructure (3.2 Q5).

### Layer 4 — 4.x (Delinquency tracking)
Q4's resolution on delinquency triggers directly feeds delinquency escalation pipeline design. The delinquency subsystem (`shared/schema.ts:2095-2131`, `shared/schema.ts:2961-3007`) must know when to re-evaluate owner status after rule-posted charges.

### Layer 5 — 5.1 (Cleanup)
If Q1 or Q2 results in table deprecation (e.g., retiring `hoaFeeSchedules`), the data migration and legacy redirect cleanup are Layer 5 scope. The archived legacy redirect `/app/financial/fees -> /app/financial/foundation` (`3.2-route-restructure.md:243`) may need updating.

### Layer 5 — 5.x (Automation observability)
Q3's resolution on execution pipeline consolidation affects the automation sweep logging and observability surfaces. A unified run log would surface in any future automation dashboard.

---

## Decision Log

**2026-04-16 — Claude documentation agent (skeleton draft)**
Audited three existing assessment-related subsystems in `shared/schema.ts` (hoaFeeSchedules at :248-261, recurringChargeSchedules at :575-613, specialAssessments at :263-277), their execution engines in `server/index.ts:109-168` (runDueRecurringCharges) and `server/assessment-installments.ts:60-186` (processSpecialAssessmentInstallments), CRUD endpoints in `server/routes.ts:2855-3073` (fee-schedules, assessments, late-fee-rules) and `server/routes.ts:3526-3721` (recurring-charges), client pages (`financial-foundation.tsx`, `financial-recurring-charges.tsx`, `financial-assessments.tsx`, `financial-billing.tsx`), portal dashboard at `server/routes.ts:11350-11397`, delinquency subsystem at `shared/schema.ts:2095-2131` and `shared/schema.ts:2961-3007`, and the automation sweep at `server/index.ts:255-270`. Cross-referenced 0.2 (six-persona model, PM-Managed Default Access Table, PM Toggle Configuration Model), 1.1 Q3 (Financials zone definition), 1.2 Q4 (`/app/financials` hub), 3.2 Q1 (Financials hub + `/app/financial/*` sub-pages preserved). Enumerated Q1-Q9 covering rule model unification, fee schedule execution gap, execution pipeline consolidation, delinquency trigger coupling, Owner Portal schedule visibility, Assisted Board access semantics, rule definition model, batch-vs-on-demand execution, and UI placement. All resolutions PENDING. Skeleton status — awaiting YCM CC + founder decisions.

**2026-04-21 — Founder (William) + Claude documentation agent (Q1/Q2/Q4 resolved)**
Q1 resolved: retire `hoaFeeSchedules`; migrate rows into `recurringChargeSchedules`; keep `specialAssessments` separate due to structurally-different installment model. Q2 resolved: migrate (Hypothesis B) — no new execution engine for fee schedules. Q4 resolved: status-quo sweep-based delinquency preserved; no inline trigger on charge post.

**2026-04-24 — Founder (William) + Claude Code (Session C lock — Q3/Q5/Q6/Q7/Q8/Q9 resolved)**
Module 4.3 fully locked. Founder directive: optimize for long-term correctness over short-term risk avoidance. **Q3 unified pipeline (A):** new `server/assessment-execution.ts` orchestrator + `assessmentRunLog` audit table + `ASSESSMENT_EXECUTION_UNIFIED` feature flag; shadow-write parity window before cutover; legacy functions retained and deprecated. **Q5 portal surface expanded:** recurring summary + upcoming special-assessment installments + per-assessment drill-in surface showing label, principal, interest rate, term, allocation method, owner's portion, payment options, total interest. Schema extensions on `specialAssessments`: `interestRatePercent`, `termMonths`, `allocationMethod` (enum: per-unit-equal | per-sq-ft | per-ownership-share | custom), `allocationCustomJson`, `paymentOptionsJson`. New `server/assessment-ownership.ts` calc module. New `GET /api/portal/assessments/:assessmentId/detail`. **Q6 full rule visibility for Assisted Board, no write;** new `assessment_rules_write` PM toggle. **Q7 canonical rule model:** `assessmentFrequencyEnum` (monthly | quarterly | annually | semi-annually | one-time); universal `graceDays` + `startDate`/`endDate`; `unitScopeMode` enum + inclusion/exclusion/unit-type-filter columns; flat amount retained (tiered-amount model filed as follow-up 4.x spec). **Q8 batch + on-demand unified** via new `POST /api/financial/rules/:ruleId/run` endpoint with `dryRun` support; legacy per-subsystem endpoints become shims, retired in 5.1. **Q9 new consolidated UI** at `/app/financial/rules` (tabs: Recurring / Special Assessments / Run History); linked from Financials hub; Foundation + Billing retain legacy banner link for one release cycle. Module 4.3 advances from IN SPEC (partial) to **SPEC LOCKED**. Build scope is multi-PR; sequencing documented in Acceptance Criteria (aggregate). Unblocks T2 `hoaFeeSchedules` retirement (independent track). Remaining Layer-4 blockers: Session A (4.1), Session D (hub-visibility rename).
